# Production Data Architecture

Legacy reference stub. Do not load during ordinary startup or local product work.

Full archived content: `docs/codex/archive/legacy-workflows/production-data-architecture.md`.

Use only when the user explicitly asks about production data architecture. Trust `package.json` before running command names from archived docs.
