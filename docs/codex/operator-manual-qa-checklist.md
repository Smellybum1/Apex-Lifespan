# Operator Manual QA Checklist

Legacy reference stub. Do not load during ordinary startup or local product work.

Full archived content: `docs/codex/archive/legacy-workflows/operator-manual-qa-checklist.md`.

Use only when the user explicitly asks for operator QA or browser write-control approval. Trust `package.json` before running command names from archived docs.
