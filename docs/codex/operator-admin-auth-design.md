# Operator Admin Auth Design

Legacy reference stub. Do not load during ordinary startup or local product work.

Full archived content: `docs/codex/archive/legacy-workflows/operator-admin-auth-design.md`.

Use only when the user explicitly asks about production operator/admin auth. Trust `package.json` before running command names from archived docs.
