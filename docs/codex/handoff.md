# Thread Handoff

Refreshed on 2026-06-11 while trimming workflow guardrails for easier iteration. Verify local state with `git status -sb` and `git log -1 --oneline` before edits.

## Startup

- Normal startup: read `AGENTS.md` and `docs/codex/project.md`.
- Read this handoff only when resuming current work or needing operator state.
- Open `docs/codex/source-candidate-workflow.md` only for source ingestion/review/curation.
- Do not read archives wholesale; search them only for targeted history.

## Operator State

- Branch: `codex/queue-claim-sources`; latest pre-trim commit was `6889e65 Record local-only handoff state`.
- GitHub push limit was lifted on 2026-06-11. Push to GitHub when useful.
- Production `main` was fast-forwarded to `70a6da5` on 2026-06-11 so Vercel production includes the current seed-mode hardening work, including `/api/health`.
- Docker Compose PostgreSQL is running and healthy as of 2026-06-11; `npm run ingest:sources -- --db-status` reports `reachable=true`.
- Public MVP/demo is live at `https://apex-lifespan.vercel.app` in seed mode.
- Current roadmap: `docs/codex/roadmap.md` targets the fully live end product.
- Production data architecture decision: use Neon Postgres through the Vercel Marketplace, initial region `aws-us-east-1`, Vercel-scoped `DATABASE_URL`, `APEX_DATA_SOURCE=database`, Prisma `migrate deploy`, and Neon restore/export backup rehearsal. Details: `docs/codex/production-data-architecture.md`.
- Operator/admin auth design decision: use Auth.js with GitHub OAuth, Prisma-backed sessions, explicit operator roles, audited `/api/operator/*` writes, public route separation, and `APEX_OPERATOR_WRITES_ENABLED` emergency write disable. Details: `docs/codex/operator-admin-auth-design.md`.
- Completed public-MVP roadmap archive: `docs/codex/archive/roadmap/2026-06-11-public-live-mvp.md`.
- A Vercel clean-build failure on `main` was fixed on 2026-06-11 by running `scripts/generate-prisma-client.ts` before `next build` and `next typegen`.

## Keep

- Public routes and dashboard remain read-only; review/admin writes stay local operator-only.
- Australia/TGA is the default lens; product-level confidence needs product-level evidence.
- Live PubMed and ClinicalTrials.gov previews are unreviewed leads; `/100` values are triage/review priority, not evidence quality.
- Source-candidate accept/reject, claim linking, and study extraction remain explicit human-reviewed local writes under `npm run ingest:sources`.
- Accepted candidates do not become public evidence until a matching curated reference is claim-linked and structurally extracted.
- Avoid peptide sourcing, compounding, reconstitution, injection, cycling, dosing, or self-administration guidance.

## Simplified

- Do not preserve long CLI output contracts in this handoff. Use `npm run ingest:sources -- --help` and `docs/codex/source-candidate-workflow.md` for current operator commands.
- Use inline plans for ordinary multi-file work. Create `docs/codex/plans/` files only for genuinely risky, unclear, schema/API/security, public-boundary, or hard-to-validate changes.
- Prefer tests/code boundaries over adding more startup instructions. Add docs only when they reduce future context load.

## Source-Candidate Snapshot

- Latest known successful summary: 15 ingestion jobs total, 49 pending candidates, 1 accepted candidate, curation handoff `total=1`.
- Pending split: PubMed AU 19 and ClinicalTrials.gov AU 30.
- Accepted candidate: scoped PubMed AU `PMID 42141930` for `claim=creatine-strength` / `intervention=creatine`, accepted with `ref-pubmed-42141930`.
- Accepted `PMID 42141930` still needs human-owned curation: status `Claim link missing`, `publicSourcePacketReady=false`.
- Duplicate scan showed one mixed PubMed identity group for `PMID 42141930`: one pending unscoped row plus one accepted scoped `creatine-strength` row.
- Review overview had 9 pending groups. Useful entry point: `npm run ingest:sources -- --candidate-review-overview --candidate-review-overview-limit 10`.
- Review flags had 3 flagged top groups: two broad `vitamin-d-deficiency` safety-query groups and one `creatine-lifespan` low-title-overlap group.

## Latest Local Validation

- Startup docs and repo-local workflow skill re-read.
- `git status -sb`
- `git log -3 --oneline`
- `docker compose ps` failed because Docker Desktop was not running.
- `npm run ingest:sources -- --db-status` failed because PostgreSQL was unreachable at `localhost:5432`.
- Roadmap step 1 completed: `git status -sb` showed a clean branch ahead of origin by 3 commits, and `git log -3 --oneline` confirmed the latest local commits.
- Roadmap step 2 completed after launching Docker Desktop: `docker compose ps`, `npm run db:migrate`, `npm run db:seed`, and `npm run ingest:sources -- --db-status` passed.
- Roadmap step 3 completed: README selects `APEX_DATA_SOURCE=seed` for the public MVP/demo and keeps managed PostgreSQL for a later production-data milestone.
- Roadmap step 4 completed: seed-mode desktop/mobile browser review passed, public product-card brand copy changed from `Seed example` to `Demo profile`, `npm run db:seed` refreshed local data, and targeted dashboard/source-packet/seed-integrity tests passed.
- Roadmap step 5 completed by explicit MVP deferral: `npm run ingest:sources -- --candidate-curation-handoff` still reports accepted `PMID 42141930` as `Claim link missing` with `publicSourcePacketReady=false`, so it stays local curation backlog and is not promoted into the public MVP.
- Roadmap step 6 completed: public route/read-only boundary tests passed for live-source routes, dashboard live-preview fetches, public-safe errors, no-store/noindex headers, and source-candidate workflow separation.
- Roadmap step 7 completed: README selects manual Vercel CLI deployment from the local checkout as the no-GitHub path and documents env, build/local smoke commands, public smoke targets, and rollback. After GitHub push access was restored, the preferred path became GitHub import to Vercel after pushing this branch.
- Roadmap step 8 completed: `.env.example` and README document public-demo `APEX_DATA_SOURCE=seed`; public deploy should omit `DATABASE_URL` and all local Codex sidecar variables.
- Roadmap step 9 completed for the current local build: `npm run test`, `npm run lint`, `npm run dev:stop`, `npm run typecheck`, `npm run build`, and `npm audit` passed.
- Roadmap step 10 completed for the current seed-mode production build: desktop/mobile screenshots and PubMed/ClinicalTrials.gov live-preview smokes passed, then `npm run dev:stop` stopped the production server.
- Public MVP roadmap steps 11-13 completed: `npm run smoke:public-mvp -- https://apex-lifespan.vercel.app` passed homepage caveats, PubMed live preview, ClinicalTrials.gov live preview, and invalid-term guards; launch handoff is `docs/codex/public-mvp-launch-handoff.md`; successor roadmap is `docs/codex/roadmap.md`.
- Vercel clean-build fix validation: with `DATABASE_URL` removed and a missing dotenv path, `npx tsx scripts/generate-prisma-client.ts` generated Prisma Client successfully using the build-time placeholder URL. `npm run lint`, `npm run typecheck`, and `npm run build` passed afterward.
- Fully-live roadmap step 1 completed: clean synced `codex/queue-claim-sources` branch, latest commits `30d6830`, `762f5cb`, `9632d8f`, and `npm run smoke:public-mvp -- https://apex-lifespan.vercel.app` passed.
- Fully-live roadmap step 2 completed: production data architecture documented in `docs/codex/production-data-architecture.md`; docs diff review and `git diff --check` used for validation.
- Fully-live roadmap steps 3-4 blocked: managed Neon database and Vercel secrets require operator console access; local `.vercel` link is absent, Vercel CLI is not available on PATH, and `npm run db:validate` passed.
- Step 3 readiness slice: added sanitized `npm run production:readiness`; current local report is blocked as expected for missing managed `DATABASE_URL`, missing `APEX_DATA_SOURCE=database`, no `.vercel/project.json`, missing operator auth secrets, plus warnings for Vercel CLI and NCBI metadata. It reports committed migrations present, no private env files tracked, no local Codex sidecar secrets configured, and operator writes disabled. Validation passed: readiness tests, readiness report, `npm run db:validate`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
- Step 3 migration rehearsal slice: added dry-run-default `npm run production:migration-rehearsal`, which requires a managed non-local `DATABASE_URL`, `APEX_DATA_SOURCE=database`, committed migrations, `APEX_MIGRATION_REHEARSAL_TARGET=non-production`, and `--apply` before running `npm run db:validate` and `npm run db:migrate:deploy`. It refuses production targets and keeps command results sanitized. `npm run production:readiness` now also blocks on missing `APEX_MIGRATION_REHEARSAL_PASSED_AT`; current local dry run is blocked for missing database URL, database mode, and rehearsal target, and `npm run production:migration-rehearsal -- --apply` fails closed with no command results. Validation passed: rehearsal/production-readiness/launch-readiness tests, `npm run db:validate`, dry run, fail-closed apply, `npm run production:readiness`, `npm run launch:readiness`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
- Step 3 production provisioning worksheet slice: `npm run production:readiness` now includes a read-only worksheet grouping blockers, warnings, ready checks, and the next operator-owned action without printing secret values. Current local report remains blocked with 5 blockers, 2 warnings, and 4 ready checks; first next action is configuring a managed Neon/Vercel `DATABASE_URL`. Validation passed: production/launch-readiness tests, `npm run production:readiness`, `npm run launch:readiness`, `npm run db:validate`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
- Step 3 production provisioning checklist slice: added `docs/codex/production-provisioning-checklist.md` and made `npm run production:readiness` verify it as a ready local artifact. Current local report remains blocked on the same 5 external checks, has 2 warnings, and now has 5 ready checks including the checklist; launch readiness still points to production data/secrets as the first launch blocker. Validation passed: full tests, production/launch readiness, `db:validate`, `typecheck`, `build`, `lint`, `npm audit`, and `git diff --check`.
- Step 3 Vercel env packet slice: expanded `docs/codex/production-provisioning-checklist.md` with a value-free variable packet for Preview/staging and Production, explicit local sidecar variables that must stay out of Vercel, and evidence variables that must not be recorded until their matching checklist item is complete. Production readiness remains blocked on external database/secrets setup. Validation passed: `npm run production:readiness`, `npm run launch:readiness`, `npm run db:validate`, and `git diff --check`.
- Step 3 GitHub-imported Vercel readiness slice: `npm run production:readiness` now accepts either local `.vercel/project.json` or reviewed `APEX_VERCEL_PROJECT_CONFIGURED_AT` evidence after confirming the GitHub-imported Vercel project, repository, branch, and production domain. A temporary-env probe with that evidence dropped only the Vercel project blocker; managed database, database mode, migration rehearsal, and operator auth secrets still block. Validation passed: production-readiness/env-template tests, current and temporary-env production readiness, and launch readiness.
- Step 3 staging-first dashboard sequence slice: clarified `docs/codex/production-provisioning-checklist.md` after the public seed demo was confirmed working. The checklist now keeps Production seed mode during provisioning, creates Preview/staging database mode first, rehearses migrations against a non-production managed database, and delays Production `APEX_DATA_SOURCE=database` until backup/restore, rollback, and database-mode QA evidence exists. Validation passed: `npm run production:readiness`, `npm run launch:readiness`, `npm run db:validate`, and `git diff --check`.
- Step 3 production readiness command packet slice: `npm run production:readiness` now emits `worksheet.copySafeCommands` for refreshing readiness, planning migration rehearsal, explicitly applying non-production rehearsal, validating Prisma, and refreshing launch readiness. The apply command is labeled `explicit-apply` and remains blocked unless the non-production managed database rehearsal gates are satisfied. Validation passed: production/launch readiness tests, `npm run production:readiness`, `npm run launch:readiness`, `npm run db:validate`, `npm run lint`, `npm run typecheck`, `npm run build`, and `git diff --check`.
- Step 3 dashboard-managed evidence slice: `npm run production:readiness` now accepts `APEX_VERCEL_DATABASE_CONFIGURED_AT`, `APEX_VERCEL_DATABASE_MODE_CONFIGURED_AT`, and `APEX_VERCEL_OPERATOR_AUTH_CONFIGURED_AT` as reviewed, value-free evidence for Vercel-managed database/data-mode/auth variables when secret values should stay out of the local shell. `npm run launch:readiness` snapshots env before data helpers can load local `.env`, so its production next action follows the stable evidence snapshot. Validation passed: production/env/launch tests, production readiness, launch readiness, `db:validate`, `lint`, `typecheck`, `build`, and `git diff --check`.
- Step 4 strict database fallback slice: `APEX_DATA_SOURCE=database` now fails closed when PostgreSQL is reachable but has no dashboard interventions or claims instead of silently rendering seed fallback data. Auto/local mode still uses the seed fallback for an empty database. Production database-mode browser review remains blocked on managed database provisioning. Validation passed: dashboard data-source tests, full `npm run test`, `npm run production:readiness`, `npm run launch:readiness`, `npm run db:validate`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
- Step 4 public unavailable-state slice: homepage dashboard data-load failures now render a sanitized read-only unavailable state with privacy/terms links, no seed fallback badge, and no raw database error details. Production database-mode browser review remains blocked on managed database provisioning. Validation passed: page-boundary tests, full `npm run test`, `npm run launch:readiness`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
- Fully-live roadmap step 5 completed: `docs/codex/operator-admin-auth-design.md` documents auth provider, roles, protected routes, audit fields, boundaries, and emergency disable/rollback behavior; public read-only boundary tests are the validation gate.
- Fully-live roadmap step 6 in progress: added plan `docs/codex/plans/2026-06-11-authenticated-operator-surfaces.md`, Auth.js-compatible Prisma auth tables, operator profile roles/status, and `OperatorAuditEvent`; local migration `20260611131000_operator_auth_foundation` applied with `npm run db:migrate`; public read-only boundary tests and `npm run typecheck` passed.
- Step 6 authorization helper slice: `src/lib/operator/authorization.ts` gates role/status/write permissions, defaults operator writes closed unless `APEX_OPERATOR_WRITES_ENABLED=true`, and is covered by `src/lib/operator/authorization.test.ts`; targeted public-boundary tests and `npm run typecheck` passed.
- Step 6 operator shell slice: added Auth.js v5 beta plus `@auth/prisma-adapter`, fail-closed `src/auth.ts`, `src/lib/operator/session.ts`, `/operator` route, and `.env.example` auth placeholders; no source-candidate review queue or mutating controls are exposed yet. Validation passed: operator tests, public-boundary tests, `npm run typecheck`, `npm run build`, `npm run lint`, and `npm audit`.
- Step 6 read-only queue slice: added `src/lib/operator/review-queue.ts` and wired `/operator` to show pending candidate rows only for active reviewer+ principals. No mutating route or write control was added. Validation passed: operator tests, public-boundary tests, sequential `npm run typecheck`, `npm run build`, `npm run lint`, and `npm audit`.
- Step 6 audited action wrapper slice: added `src/lib/operator/audit.ts` and `src/lib/operator/source-candidate-actions.ts` for candidate decisions, claim linking, and study extraction. Wrappers require operator permission, honor the write-disable flag, and append audit events; no public mutating routes or browser write controls were added. Validation passed: wrapper tests, targeted operator/public-boundary tests, `npm run typecheck`, `npm run build`, and `npm audit`.
- Step 6 operator bootstrap slice: added dry-run-default `npm run operator:bootstrap`; writes require `--apply`, matching `--confirm-email`, and `APEX_OPERATOR_WRITES_ENABLED=true`, then upsert `User`/`OperatorProfile` and append `OperatorAuditEvent`. Dry run for `owner@example.com` passed without DB writes. Validation passed: bootstrap tests, targeted operator/public-boundary tests, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
- Step 6 auth route slice: mounted Auth.js handlers at `/api/auth/[...nextauth]` for GitHub OAuth/operator sessions. Added dedicated auth-route boundary coverage and narrowed the live-source route scan to exclude only that exact auth route while keeping source-candidate persistence forbidden from public routes. Validation passed: auth-route/public-boundary/operator-session tests, broader operator/public-boundary bundle, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
- Step 6 operator auth-control slice: added explicit `/operator` GitHub sign-in and sign-out server-action forms backed by Auth.js. Auth-unavailable deployments still fail closed, disabled operators can sign out, active operators can sign out, and the candidate review queue remains read-only with no browser accept/reject, claim-link, extraction, or promotion controls. Validation passed: operator auth-control tests, auth-route/public-boundary/operator-session tests, broader operator/public-boundary bundle, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
- Step 6 operator readiness slice: added sanitized read-only `npm run operator:readiness`. Current report shows 8 ready local checks and remains blocked on database-backed GitHub auth variables, active-operator evidence, non-production write QA, manual operator-flow QA, and browser-write-control approval. Validation passed: readiness tests, `npm run operator:readiness`, targeted operator/public-boundary tests, full `npm run test` (38 files, 390 tests), `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
- Step 6 manual QA checklist slice: added `docs/codex/operator-manual-qa-checklist.md`; `npm run operator:readiness` now verifies it exists and points manual-QA blockers to it. Validation passed: readiness tests, `npm run operator:readiness`, targeted operator/public-boundary tests, `npm run typecheck`, `npm run lint`, `npm audit`, and `git diff --check`.
- Step 6 operator readiness worksheet slice: `npm run operator:readiness` now includes a read-only worksheet grouping ready local artifacts, ready evidence, blockers, warnings, and the next operator-owned action. Current local report has 7 ready local artifacts, 1 ready evidence item, and 5 blockers; no browser write controls were exposed. Validation passed: operator/launch-readiness tests, targeted operator/public-boundary tests, `npm run operator:readiness`, `npm run launch:readiness`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
- Step 6 browser write controls slice: added approval-gated browser controls for candidate accept/reject, claim linking, and study extraction plus fail-closed form parsers. Controls require active operator permission, `APEX_OPERATOR_WRITES_ENABLED=true`, `APEX_OPERATOR_NONPROD_WRITE_QA_AT`, `APEX_OPERATOR_FLOW_QA_REVIEWED_AT`, and `APEX_OPERATOR_BROWSER_WRITE_CONTROLS_APPROVED_AT`; default public/seed deployments remain locked. Current `npm run operator:readiness` has 10 ready local checks and 5 external blockers. Validation passed: browser-control/action tests, operator readiness tests, audited wrapper tests, auth-route/public-boundary tests, and `npm run operator:readiness`.
- Step 6 operator smoke slice: added `npm run operator:smoke -- <base-url>` for the anonymous `/operator` boundary. It checks security headers, closed auth state, and absence of authenticated queue/promotion/write-control text. `npm run operator:readiness` now counts it as a ready local artifact; current local report has 11 ready checks and the same 5 blockers. Live seed-mode smoke passed against `https://apex-lifespan.vercel.app --expect-auth-unavailable`. Validation passed: full tests, operator-smoke/operator-readiness/public-smoke tests, operator/launch readiness, typecheck, build, lint, audit, and `git diff --check`.
- Step 6 operator audit-trail slice: added bounded `src/lib/operator/audit-trail.ts` and a protected read-only `/operator` audit trail panel for active operators with `audit:read`. The panel shows recent action, actor, target, timestamp, and short note previews without dumping audit JSON summaries; anonymous operator smoke now treats `Audit trail` as authenticated content. `npm run operator:readiness` now counts 12 ready local checks and the same 5 external/manual QA blockers. Validation passed: audit-trail/readiness/operator-source/smoke tests, full tests, operator/launch readiness, typecheck, build, lint, audit, and `git diff --check`.
- Step 6 operator role-gate render-test slice: added async render tests for auth-unavailable, anonymous, auditor, reviewer, and admin `/operator` states. Active auditors now do not see the candidate review queue shell; reviewers see the queue plus audit trail without write buttons while controls are locked; admins see promotion readiness without promotion controls while controls are locked. External/manual QA blockers are unchanged. Validation passed: operator page render tests, targeted operator boundary tests, full tests, operator/launch readiness, typecheck, build, lint, audit, and `git diff --check`.
- Step 6 dashboard-managed auth evidence slice: `npm run operator:readiness` accepts `APEX_VERCEL_DATABASE_CONFIGURED_AT` plus `APEX_VERCEL_OPERATOR_AUTH_CONFIGURED_AT` as reviewed value-free evidence for database-backed GitHub auth when Vercel dashboard secrets should not be copied locally. Current local readiness still has 12 ready checks and 5 blockers; a temporary-env probe with both evidence keys dropped the auth blocker and left active-operator, non-production write QA, manual flow QA, and browser-write-control approval blocked. Validation passed: operator readiness/env/launch tests, current and temporary-env operator readiness, launch readiness, targeted operator/public-boundary tests, full tests, typecheck, build, lint, and `git diff --check`.
- Step 6 operator command-packet slice: `npm run operator:readiness` now emits `worksheet.copySafeCommands` for readiness refresh, anonymous operator boundary smoke modes, dry-run operator bootstrap planning, and aggregate launch readiness. The packet contains no `--apply` command. The emitted bootstrap command returned `applied=false`/`dryRun=true`, and live seed-mode operator smoke passed with `--expect-auth-unavailable`. Validation passed: operator readiness/bootstrap/smoke/public-boundary tests, operator readiness, emitted dry-run bootstrap, live operator smoke, launch readiness, full tests, typecheck, build, lint, and `git diff --check`.
- Step 6 remaining blocker: browser write controls and manual operator-flow QA require configured database-backed GitHub OAuth or reviewed Vercel dashboard evidence, a seeded active operator account, and `APEX_OPERATOR_WRITES_ENABLED=true` in non-production first.
- Step 7 in progress: added curation promotion plan, read-only promotion assessment, and `npm run promotion:dry-run -- --pmid <id>`. Dry run for `PMID 42141930` found the accepted/human-reviewed candidate but correctly blocked promotion until claim link, structured extraction, and `publicSourcePacketReady=true` are complete. Promotion/source-packet/public-boundary tests, `npm run typecheck`, `npm run build`, `npm run lint`, and `npm audit` passed.
- Step 7 promotion readiness panel slice: added a bounded promotion-readiness snapshot helper and a protected `/operator` read-only panel for admin+ operators. It shows accepted-candidate readiness counts, next actions, and blockers without promote, accept/reject, claim-link, extraction, or other browser write controls. Dry run for `PMID 42141930` still passed as blocked for missing claim link, structured extraction, and `publicSourcePacketReady=true`. Validation passed: promotion readiness tests, operator auth-control tests, auth-route/public-boundary/operator-session tests, broader operator/public-boundary bundle, `npm run promotion:dry-run -- --pmid 42141930`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
- Step 7 curation promotion checklist slice: added `docs/codex/curation-promotion-checklist.md` and linked it from `docs/codex/source-candidate-workflow.md`. Dry run for `PMID 42141930` still blocks on claim link, structured extraction, and `publicSourcePacketReady=true`. Validation passed: promotion/source-packet/public-boundary tests, `npm run promotion:dry-run -- --pmid 42141930`, and `git diff --check`.
- Step 7 dry-run worksheet slice: local `npm run promotion:dry-run -- --pmid 42141930` now includes a read-only human worksheet that names the missing claim link, missing structured extraction, and rerun step before promotion review. It still blocks promotion with `publicSourcePacketReady=false` and adds no public route, browser write control, or auto-promotion path. Validation passed: promotion assessment tests, source-packet/public-boundary tests, dry run, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
- Step 7 audited promotion-control slice: added `promoteSourceCandidatePublicEvidenceAsOperator` and `promoteCandidateFromBrowserForm`. Promotion requires admin+ permission, `APEX_OPERATOR_WRITES_ENABLED=true`, existing browser-control QA evidence, a ready dry-run assessment, and a human promotion note; it marks only the target claim `Human reviewed`, records `lastReviewedAt`, and appends `sourceCandidate.publicEvidencePromotion`. The `/operator` form renders only when the row is ready and promotion browser controls are enabled. Current `PMID 42141930` dry run remains blocked on claim link, structured extraction, and `publicSourcePacketReady=false`. Validation passed: full tests, targeted operator/promotion and public-boundary tests, dry run, operator/launch readiness, lint, audit, typecheck, build, and `git diff --check`.
- Step 7 promotion inspection-command slice: `npm run promotion:dry-run -- --pmid 42141930` now emits `worksheet.readOnlyCommands` for review packet, reference matches, siblings, curation status, curation draft, and rerunning the dry run using shell-safe `b64:` keys. The emitted curation-status command passed and confirmed `Claim link missing`, `publicSourcePacketReady=false`, and `studies=0` with no writes. Validation passed: full tests, promotion/action tests, dry run, emitted curation-status command, launch readiness, typecheck, build, lint, audit, and `git diff --check`.
- Step 7 promotion evidence-checklist slice: `npm run promotion:dry-run -- --pmid 42141930` now emits `worksheet.requiredEvidence` for accepted candidate, human review, traceable accepted reference, claim link, structured extraction, and public packet readiness. Current output shows the first 3 gates ready and the claim-link, structured-extraction, and public-packet gates blocked; no write path or public route changed. Validation passed: promotion tests, source-candidate/public-boundary tests, dry run, launch readiness, full tests, typecheck, lint, audit, build, and `git diff --check`.
- Step 7 remaining blocker: using promotion still waits for human-owned claim link and structured extraction for `PMID 42141930`, configured operator auth, non-production manual QA, and browser-control approval.
- Step 8 in progress: added scheduled ingestion design plus `npm run ingest:scheduled-dry-run`; dry run is read-only, reports queued/running/failed jobs and planned batch size, and explicitly keeps `noAutoPromotion=true`. Local dry run passed with no queued jobs. Hosted cron/retry automation remains blocked on production DB/secrets, monitoring, NCBI metadata, and operator approval.
- Step 8 scheduler policy slice: dry run now includes a structured source policy review: PubMed `retmax` cap 20, ClinicalTrials.gov `pageSize` cap 20, scheduler default/max batch 1/5, `automaticRetries=false`, `noAutoPromotion=true`, and missing NCBI metadata by variable name only. Local `npm run ingest:scheduled-dry-run` passed with no queued/running/recent failed jobs and `hostedCronReady=false` because `NCBI_EMAIL` is missing. Validation passed: scheduler/public-boundary tests, `npm run ingest:scheduled-dry-run`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
- Step 8 guarded runner slice: added local `npm run ingest:scheduled-run`, which passes `--apply` but still fails closed unless `APEX_SCHEDULED_INGESTION_WRITES_ENABLED=true`, NCBI metadata is complete, and no ingestion job is already running. The runner processes only bounded queued source-candidate jobs with PubMed/ClinicalTrials.gov caps of 20, `automaticRetries=false`, and `noAutoPromotion=true`; tests use a mocked runner, not upstream APIs. Local `npm run ingest:scheduled-run` correctly failed closed with exit code 1 because the scheduled ingestion write gate is disabled. Validation passed: scheduler/public-boundary tests, `npm run ingest:scheduled-dry-run`, fail-closed `npm run ingest:scheduled-run`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
- Step 8 hosted readiness slice: `npm run ingest:scheduled-dry-run` now reports a hosted-cron readiness contract and keeps `hostedCronReady=false` until managed database mode, managed `DATABASE_URL`, scheduled ingestion write gate, ingestion alerts, explicit cron approval, and NCBI metadata are configured. No hosted HTTP write endpoint was added. Current local dry run is blocked as expected with no queued jobs and missing hosted evidence for `DATABASE_URL`, `APEX_DATA_SOURCE=database`, `APEX_SCHEDULED_INGESTION_WRITES_ENABLED=true`, `APEX_INGESTION_ALERTS_CONFIGURED=true`, `APEX_SCHEDULED_INGESTION_CRON_APPROVED=true`, and `NCBI_EMAIL`. Validation passed: scheduler/public-boundary tests, dry run, fail-closed guarded run, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
- Step 8 failure review slice: `npm run ingest:scheduled-dry-run` now includes a sanitized `failureReview` with failed-job retry categories and dispositions, does not print raw failed-job error text, and keeps `automaticRetries=false` plus `retryAutomationReady=false`. Current local dry run reports `failedJobsReviewed=0`. Validation passed: scheduler/public-boundary tests, dry run, fail-closed guarded run, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
- Step 8 scheduler worksheet slice: `npm run ingest:scheduled-dry-run` now includes a read-only `worksheet` grouping ready local controls, queued work, hosted-cron blockers, retry-policy blockers, warnings, and the next operator-owned action. Current local dry run has 0 queued jobs, 0 failed jobs, ready dry-run/source-cap/no-auto-promotion/manual-retry controls, and blockers for `NCBI_EMAIL`, hosted cron evidence, and retry policy approval. `npm run ingest:scheduled-run` still fails closed with exit code 1 because `APEX_SCHEDULED_INGESTION_WRITES_ENABLED=true` is not set. Validation passed: scheduler/launch-readiness tests, dry run, fail-closed guarded run, and `npm run launch:readiness`.
- Step 8 scheduler dedupe-review slice: `npm run ingest:scheduled-dry-run` now includes a bounded read-only `dedupeReview` for duplicate PubMed/NCT source identities, decision counts, mixed-decision state, and a read-only inspection command. Current local dry run reports 1 duplicate PubMed identity group for `PMID 42141930` with 1 pending and 1 accepted candidate; the generated `npm run ingest:sources -- --candidates --candidate-duplicates --candidate-source pubmed --candidate-external-id 42141930` command passed and made no writes.
- Step 8 retry-policy evidence slice: added `docs/codex/scheduled-ingestion-retry-policy.md` and `APEX_INGESTION_RETRY_POLICY_APPROVED_AT`. Dry run now reports `failureReview.retryPolicy`, keeps `automaticRetries=false`, and only marks retry readiness true after policy approval is recorded and there are no recent failed jobs awaiting review. Current local dry run remains blocked on `NCBI_EMAIL`, hosted cron evidence, and retry-policy approval; guarded run still fails closed with no writes.
- Step 9 in progress: added `npm run coverage:review`; current report shows 7/7 structurally complete source packets, 0 human-reviewed claims, 7 unreviewed draft claims, and psyllium with no claim coverage. Coverage/source-packet/seed-integrity tests, `npm run typecheck`, `npm run build`, `npm run lint`, and `npm audit` passed. Human review/curation is required before marking claims reviewed or adding public evidence.
- Step 9 review backlog slice: `npm run coverage:review` now includes a ranked read-only `claimReviewBacklog` and `interventionGaps`. Current local report ranks 7 unreviewed complete source packets for human review, with top priority ties for `bpc-157-injury-healing` and `vitamin-d-deficiency`, and keeps `psyllium` as the only intervention gap. No claim review statuses or public evidence records were changed. Validation passed: coverage/source-packet/seed-integrity tests, dashboard/public-boundary tests, `npm run coverage:review`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
- Step 9 review sampling slice: `npm run coverage:review` now includes a read-only `reviewSamplingPlan` for the next human review batch. Current local plan samples 3 of 7 complete unreviewed packets in priority order: `bpc-157-injury-healing`, `vitamin-d-deficiency`, and `creatine-strength`, including linked reference IDs and structured study IDs. No claim review statuses or public evidence records were changed. Validation passed: coverage/source-packet/seed-integrity tests, dashboard/public-boundary tests, `npm run coverage:review`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
- Step 9 coverage worksheet slice: `npm run coverage:review` now includes a read-only `worksheet` grouping the first 3-claim review batch, all 7 ready source packets, remaining backlog, and the `psyllium` intervention gap. It keeps coverage work human-owned and does not change claim review statuses or public evidence. Current report still has 0/7 human-reviewed claims, 7 ready source packets, and 1 intervention gap.
- Step 9 sampled review context slice: sampled `coverage:review` items now include source-packet status, population/dose-form/duration boundary fields, visible confidence/final-label/review-status fields, and a short human checklist with safety, AU/TGA, peptide, and traceability reminders where relevant. No claim review statuses, source packets, public evidence, or write paths changed. Validation passed: coverage/source-packet/seed-integrity/dashboard/public-boundary/launch-readiness tests, `npm run coverage:review`, `npm run launch:readiness`, full `npm run test`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
- Step 10 completed: added read-only product-level AU/TGA verification helpers, `npm run regulatory:review`, and dashboard product chips that show product-level state plus AU confidence. Current report shows 5 intervention-level statuses, 2 product-level statuses, 2 very-low-confidence unknown product statuses, no missing product statuses, and no stale statuses. Validation passed: AU/TGA verification/dashboard/regulatory tests, public read-only boundary tests, `npm run regulatory:review`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
- Step 11 in progress: added public `/privacy` and `/terms`, dashboard links, and `docs/codex/operations-runbook.md`. Validation passed: legal page/dashboard render tests, public read-only boundary tests, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`. Remaining external proof: uptime/error monitoring, Vercel deployment alerts, Neon backup/restore rehearsal, alert test, and rollback drill evidence.
- Step 11 operations readiness slice: added read-only `npm run operations:readiness` with sanitized local/external evidence checks. Current local report has privacy, terms, and operations runbook ready; it remains blocked on external proof variables for uptime monitoring, error monitoring, deployment alerts, scheduled-ingestion alerts, database backups, backup restore rehearsal, rollback drill, and alert test. Validation passed: operations-readiness tests, `npm run operations:readiness`, targeted operations/legal/public-boundary tests, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
- Step 11 operations worksheet slice: `npm run operations:readiness` now includes a read-only worksheet grouping ready local artifacts, missing external evidence, ready external evidence, and the next operator-owned evidence action. Current local report still has 3 ready local artifacts and 8 missing external operations evidence items; first next action is uptime monitoring. Validation passed: operations/launch-readiness tests, `npm run operations:readiness`, `npm run launch:readiness`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
- Step 11 operations drill checklist slice: added `docs/codex/operations-drill-checklist.md` and made `npm run operations:readiness` verify it as a local artifact. Current local report has 4 ready local artifacts and still blocks on the 8 external evidence items; first next action remains uptime monitoring. Validation passed: operations/launch-readiness tests, `npm run operations:readiness`, and `npm run launch:readiness`.
- Step 11 health endpoint slice: added public read-only `/api/health` for lightweight uptime monitoring. It returns no-store/noindex static JSON, does not check the database or read secrets, and is covered by public smoke plus read-only boundary tests. `npm run operations:readiness` now reports 5 ready local artifacts and still blocks on the 8 external operations evidence items; first next action is configuring uptime monitoring for `/api/health`.
- Step 11 uptime evidence hygiene slice: `npm run operations:readiness` rejects `APEX_UPTIME_MONITORING_URL` values that are unparsable, non-http(s), local-only, or contain credentials/query strings/fragments, without echoing token-bearing URLs. Clean sanitized monitor URLs remain accepted. Validation passed: operations-readiness tests, operations readiness, and launch readiness.
- Step 11 live deploy slice: fast-forwarded `main` to `70a6da5` and pushed to GitHub. Vercel caught up on the third smoke attempt; a clean follow-up `npm run smoke:public-mvp -- https://apex-lifespan.vercel.app` passed homepage, legal pages, anonymous `/operator`, `/api/health`, PubMed and ClinicalTrials.gov previews, and invalid-term guards.
- Step 12 in progress: added conservative global security headers in `next.config.mjs` plus `src/app/security-headers-config.test.ts`; strict script/style CSP and cross-origin isolation remain deferred until browser QA. Validation passed: header config/public-boundary tests, full `npm run test` (31 files, 361 tests), `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`. Remaining external proof: production database mode, public smoke after deploy, browser QA, accessibility/performance checks, and security/monitoring evidence selected for launch.
- Step 12 smoke/visual slice: expanded `npm run smoke:public-mvp -- <url>` to check homepage, privacy, terms, global security headers, live-source previews, and invalid-term guards. Local production seed-mode smoke passed against `http://127.0.0.1:3000`; Playwright desktop/mobile screenshots in `output/playwright/` were reviewed without new header/legal-link overlap. Validation passed: targeted render/header tests, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
- Step 12 accessibility slice: local seed-mode axe initially found dashboard contrast and definition-list/landmark issues; fixed amber contrast, dashboard micro-label contrast, internal `aside` landmark usage, and `MiniStat` semantics. Final axe pass found 0 violations on `/`, `/privacy`, and `/terms`; smoke and Playwright desktop/mobile screenshots passed afterward. Validation passed: `npm run test`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
- Step 12 performance slice: local seed-mode Lighthouse pass for `/` completed after pointing Windows temp paths at `output/lighthouse-temp`; scores were Performance 96 and Accessibility 100. Report artifact is local ignored output at `output/playwright/lighthouse-home.json`.
- Step 12 smoke hardening slice: `npm run smoke:public-mvp -- <url>` now accepts either explicit seed or database data-source badges and checks `/operator` as an anonymous closed boundary with no review queue, promotion, or sign-out content visible. Local production database-backed smoke passed against `http://127.0.0.1:3000`. Live public smoke initially failed because production `main` did not include the security-header work; after fast-forwarding and pushing `main`, `npm run smoke:public-mvp -- https://apex-lifespan.vercel.app` passed on redeploy attempt 4 with homepage headers, legal pages, anonymous operator closure, live previews, and invalid-term guards. Validation passed: local smoke, public smoke, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
- Step 12 smoke testability slice: `scripts/smoke-public-mvp.ts` now exports an importable smoke runner while preserving the CLI entrypoint, with mock-server tests for database-backed public smoke and anonymous operator content leakage. Validation passed: smoke test, targeted operator/public-boundary tests, local CLI smoke against `http://127.0.0.1:3000`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
- Step 12 live browser QA slice: captured and visually reviewed seed-mode production screenshots at `output/playwright/apex-public-desktop-2026-06-11.png` and `output/playwright/apex-public-mobile-2026-06-11.png`. Desktop and mobile showed no obvious header/control overlap or text collisions across the public dashboard, legal links, anonymous operator boundary, evidence map, claim table, safety cards, product panel, and source queue. Production database-mode browser QA remains open.
- Step 12 live accessibility/performance slice: production seed-mode axe found 0 violations on `/`, `/privacy`, and `/terms` with JSON saved at `output/playwright/axe-public-2026-06-11.json`. Production seed-mode Lighthouse for `/` saved `output/playwright/lighthouse-public-home-2026-06-11.json` and scored Performance 98, Accessibility 100, Best Practices 100, and SEO 100. Production database-mode accessibility/performance confirmation remains open.
- Step 12 refreshed live smoke slice: `npm run smoke:public-mvp -- https://apex-lifespan.vercel.app` passed after the user-visible Vercel deployment loaded, covering homepage, privacy, terms, anonymous operator boundary, PubMed and ClinicalTrials.gov live previews, and invalid-term guards. This confirms the current public demo works in seed mode; production database-mode QA remains open.
- Step 13 launch readiness slice: added `npm run launch:readiness`, a read-only aggregate launch gate report across production readiness, operator readiness, operations readiness, scheduled ingestion, promotion readiness, evidence coverage, public smoke, admin-flow smoke, launch approval, and post-launch review scheduling. Current local report is blocked as expected with 10 blocked checks: production database/secrets, operator QA, operations evidence, hosted cron/retry policy, accepted-candidate promotion blockers, human-reviewed coverage, fully-live public/admin smokes, launch approval, and post-launch review scheduling. Validation passed: launch-readiness tests, targeted readiness/scheduler/promotion/coverage tests, `npm run launch:readiness`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
- Step 13 env template slice: `.env.example` now documents fully-live evidence variables for operator QA, migration rehearsal, operations evidence, launch smoke/approval, and post-launch review with commented/false defaults. Added `src/lib/env-example.test.ts` so readiness evidence keys stay visible in the operator template. Validation passed: env-example test, `npm run typecheck`, `npm run lint`, and `git diff --check`.
- Step 13 launch checklist slice: added `docs/codex/fully-live-launch-checklist.md` and a launch-readiness local artifact gate for it. `npm run launch:readiness` now reports the checklist ready while remaining blocked on the 10 real launch gates. Validation passed: launch-readiness tests, `npm run launch:readiness`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
- Step 13 launch readiness worksheet slice: `npm run launch:readiness` now includes a human-owned worksheet grouping blocked gates, ready gates, warning gates, and the next launch action. Current local report has 1 ready gate (`fully-live-launch-checklist`) and 10 blocked gates; next launch action remains production data/secrets. Validation passed: launch-readiness tests and `npm run launch:readiness`.
- Step 13 post-launch review template slice: added `docs/codex/post-launch-review-template.md` and made `npm run launch:readiness` verify it as a local artifact. Current local report has 2 ready gates (`fully-live-launch-checklist`, `post-launch-review-template`) and 10 blocked gates; next launch action remains production data/secrets. Validation passed: launch-readiness tests and `npm run launch:readiness`.
- Step 13 nested next-action slice: `npm run launch:readiness` now bubbles up concrete nested readiness next actions instead of generic blocked-gate wording. Current local report still has 2 ready gates and 10 blocked gates, but the top-level next launch action now points directly to using a managed non-local PostgreSQL target before production migration rehearsal. Validation passed: launch-readiness tests, `npm run launch:readiness`, full `npm run test`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.

Historical source-candidate progress lives in `docs/codex/archive/handoff/2026-06-04-source-candidate-progress.md`; search it only for targeted evidence.
