# Scheduled Ingestion Design

Legacy reference stub. Do not load during ordinary startup or local product work.

Full archived content: `docs/codex/archive/legacy-workflows/scheduled-ingestion-design.md`.

Use only when the user explicitly asks about hosted scheduled ingestion. Trust `package.json` before running command names from archived docs.
