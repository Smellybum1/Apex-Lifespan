# Roadmap: Live Operations And Evidence Product Growth

Last updated: 2026-06-14

## Target

Operate Apex Lifespan as a credible, auditable public evidence intelligence product for supplements, peptides, and healthspan interventions. The product should help users understand claim-specific evidence strength, safety context, regulatory context, uncertainty, and what would change confidence.

Apex scores scoped claims, not compounds. It must not become a naive supplement leaderboard.

Reference detail: `docs/codex/reference/evidence-product-roadmap-reference.md`.

Archived launch roadmap: `docs/codex/archive/roadmap/2026-06-13-fully-live-end-product.md`.

Archived trust-polish backlog: `docs/codex/archive/roadmap/2026-06-13-trust-polish-backlog.md`.

## Current Baseline

- Public URL: `https://apex-lifespan.vercel.app`.
- Public data mode: database-backed production reads.
- Public routes and dashboard remain read-only.
- Authenticated operator workflows are available to approved operators.
- Scheduled ingestion evidence is ready and monitored; source-candidate promotion remains explicit and human-owned.
- Post-launch review completed early on Sunday, June 14, 2026 at about 12:31 AM Australia/Brisbane; the scheduled 2:00 PM checkpoint remains useful if production DB evidence is refreshed.
- Trust-polish work shipped on 2026-06-13: methodology page, public changelog, prototype notice, composite-score tooltips, score-label directionality, evidence-depth badges, evidence-map legend, operator-mode relabeling, human-review tooltip, feedback intake, and `/interventions/[slug]` detail pages.

## Principles

- Every score applies to a specific intervention, dose/form, population, outcome, comparator, duration, and evidence base.
- Every public medical claim should remain citation-linked and uncertainty-aware.
- Draft extraction, citation checking, human review, and clinical guideline endorsement are separate concepts.
- Product quality, regulatory status, and efficacy are separate concepts.
- Peptides, research chemicals, injectables, hormone-like agents, SARMs, and drug-like geroprotectors are therapeutic/regulatory watchlist items, not ordinary supplement recommendations.
- Evidence-depth wording must distinguish "not yet extracted" from "searched and no credible evidence found."

## Operations Checkpoints

These protect the live product without blocking normal local iteration. They become blockers only if they uncover launch safety, auth, data integrity, privacy, or medical-claim trust issues.

1. [x] Complete the 24-48 hour post-launch review.
   Cadence: one-time launch checkpoint.
   Done when: `docs/codex/post-launch-review-template.md` is filled with evidence from the scheduled review, issues are recorded, and rollback/disable decisions are explicit.
   Latest state: completed early at user request on 2026-06-14 around 00:31 Australia/Brisbane. Public DB-backed smoke and anonymous operator auth smoke passed; no rollback or data restore was indicated. Follow-up blockers are operational evidence/configuration and local production DB inspection, not a public-route outage.
   Validate with: public smoke, operator smoke, operations readiness, scheduled ingestion dry-run, review notes.

2. [ ] Maintain stabilization checkpoints after meaningful iteration batches.
   Cadence: recurring before production deploys, PRs, handoffs, or larger roadmap transitions; not required before every small local edit.
   Done when: current local work is reviewed and organized without staging unrelated dirty work.
   Latest state: production hotfix `68981fe` is on `main`; after commit splitting, the branch was clean and ahead of `origin/codex/queue-claim-sources` by six organized local commits through `f343960`. Later docs-only handoff prep may be uncommitted. `npm run launch:readiness -- --summary` now defaults its read-only aggregation to silent Prisma client logging so unavailable local databases are represented as structured blocked gates without raw query-error noise.
   Validate with: `git status -sb`, focused diffs, targeted tests, `npm run lint`, `npm run typecheck`, `npm run build`, `npm audit` when relevant to the batch.

## Ordered Product Steps

1. [x] Sprint 1: finish trust polish and UI consistency.
   Latest state: immediate trust-polish backlog is implemented and archived.
   Validate with: focused render tests, accessibility spot checks, `npm run test`, `npm run lint`, `npm run typecheck`, local browser QA.

2. [ ] Sprint 2: harden the data model for evidence, scores, and review history.
   Done when: interventions, claims, source packets, extracted evidence rows, score snapshots, score history, review events, changelog entries, products, safety alerts, and regulatory statuses have stable schemas and migration coverage.
   Latest state: additive schema/migration foundations exist for source packets, source-packet references, claim-study relevance links, claim score snapshots, score history, review events, and public changelog entries. Seed backfill now upserts source packets, source-packet references, claim-study links, and score snapshots; dashboard/detail hydration reads current source-packet references, normalized claim-study links, normalized score snapshots, and normalized score history; claim packet review and source-candidate promotion write review events, score snapshots, score history, and draft changelog entries; `/changelog` can read published database changelog rows with static fallback. Intervention detail pages now expose normalized score-history provenance with score-change rationale and linked reference labels, falling back to snapshot rationale when no history row exists. Draft changelog entries now have an operator-only publication helper gated by admin evidence-promotion permission, global write enablement, a dedicated reviewed timestamp, and audit logging. Claim packet review and source-candidate public promotion now write their claim status update, review event, score snapshot, score history, draft changelog entry, and operator audit record inside one transaction. Seed normalized evidence rows now run a local integrity preflight before seed writes, and post-seed verification rehydrates seed-owned normalized rows from persisted Prisma-shaped rows before rerunning the same unit-covered state/value checks, so source packets, references, claim-study links, score snapshots, and their material state/value fields fail fast if the generated import graph, persisted rows, or values drift. Persisted claim-study rehydration now keeps stale seed-owned links visible by seed backfill note while ignoring operator-owned claim-study rows, including operator rows on existing seed claims. Strict database-mode dashboard failures now sanitize missing-table errors into approved-migration guidance while still failing closed. Approved non-production preview migration/seed smoke applied the normalized evidence and trial-alert migrations to `.env.vercel.preview.local`, seeded/backfilled preview data with seed integrity verified, confirmed no pending Prisma migrations, and reran strict read-only preview coverage/launch readiness without missing-table failures. Coverage review summaries now expose the 8 ready claim packets in priority order with copy-safe focused packet commands, env-file command templates, and human decision gates; focused claim packets now include traceable reference and structured study summaries for human review; `npm run coverage:review -- --ready-claim-packets` prints all 8 read-only human-review packets in priority order without changing review status. Remaining work: human review of the 8 complete source packets before coverage/expansion can be considered ready, plus production migration/deploy decisions remain separate.
   Validate with: Prisma migration review, `npm run db:validate`, `npm run db:generate`, data-mode smoke, dashboard/detail hydration tests.

3. [x] Sprint 3: improve trial watcher relevance and alerts.
   Done when: trial records distinguish direct match, combination product, related outcome only, wrong population, unreviewed lead, results posted, completed/no results, and terminated/unknown; trial alerts do not auto-promote evidence.
   Latest state: live ClinicalTrials.gov preview has relevance and result-state labels, plus read-only alert labels for posted results, missing results, active direct-match monitoring, registry-status review, and low-priority leads. Alert wording explicitly keeps registry rows as review leads that do not auto-promote evidence or change scores. ClinicalTrials.gov source-candidate metadata and curation drafts carry the same alert cue into human review. Additive `TrialAlert` schema/migration now provides persistent alert storage with explicit `noAutoPromotion` and optional score-history links, and a pure draft builder maps live trial results into future alert create-data shapes. Dashboard data hydration and Trial Watcher render persisted trial alerts as a read-only review queue. Operator-gated `recordTrialAlertsAsOperator` can persist reviewed alert drafts behind admin curation write permission, global write enablement, a required human note, and audit logging while keeping `noAutoPromotion` true and score history unlinked. ClinicalTrials.gov source candidates now carry relevance/result labels into metadata, the operator review queue surfaces alert cues as no-score-change review work, and an approved browser-gated operator form can record reviewed trial alerts from source candidates without accepting, extracting, promoting, or changing scores. Scheduled ingestion policy keeps ClinicalTrials.gov alert labels as source-candidate metadata until an operator explicitly records a reviewed `TrialAlert`; it does not auto-open alert rows. `npm run trial-alert:persistence-smoke` now provides a read-only dry-run fixture that verifies no-auto-promotion draft shape, while `--apply` fails closed unless a non-production `--env-file`, `--target local|preview`, and `--enable-write-gate` are supplied. Approved Preview apply smoke against `.env.vercel.preview.local` created and verified one reviewed alert, confirmed `noAutoPromotion: true`, kept score history unlinked, cleaned the alert row, and cleaned two smoke audit rows. Production deploy/migration remains a separate release decision.
   Validate with: clinical-trials integration tests, trial relevance tests, source-candidate dry-runs, changelog preview tests.

4. [ ] Sprint 4: make safety and regulatory context first-class.
   Done when: safety domains, severity labels, regional regulatory flags, peptide-specific warnings, WADA/sport-risk flags, and product-quality/regulatory separation are visible and filterable.
   Latest state: safety center and AU/TGA context exist; the dashboard evidence map now exposes safety-domain, safety-severity, safety-region, and AU/TGA-context facets, including supported sport/prohibited-sport domain filtering without adding unverified WADA claims. Safety cards show domain and severity chips, and the Safety Center now groups captured alerts into clinical-safety, product-quality, regulatory-access, and sport-eligibility domain summaries with highest severity, regions, and alert types. It also shows a read-only coverage snapshot for all safety domains so domains without reviewed local alerts are visible as gaps rather than inferred safety, plus a regional review-scope readout that groups captured safety alerts and AU/TGA rows, shows the highest reviewed safety severity per region, and shows configured regional review scopes without reviewed records as gaps, without treating absent records as clearance. A regional safety-domain matrix now cross-tabulates configured review regions against safety domains so missing region/domain cells are visible as review gaps rather than clearance. Intervention detail pages now show intervention-specific safety-domain coverage, intervention-specific regional safety/AU-TGA scope, and domain chips on individual safety alerts, including normalized region display with raw alias preservation. Common reviewed-alert region aliases such as US, UK, EU, AU, and Global now normalize into configured review scopes before coverage grouping, filtering, and alert display while preserving raw alias visibility on individual rows. `npm run regulatory:review` now includes a read-only regional safety/regulatory review gap output with captured and missing safety domains, AU/TGA row counts, region-specific next actions, workflow priority labels, and `noClearanceInferred: true` so absent regional records remain review gaps rather than clearance; the Safety Center surfaces the same priority labels and missing-domain readouts on regional review cards. BPC-157 remains visible as an unapproved peptide watchlist item, and the product label analyzer keeps product quality, efficacy evidence, and AU/TGA status separate with product-level AU/TGA and quality-signal filters. Remaining work: deeper region-specific model expansion as more reviewed alert types are added.
   Validate with: safety/regulatory tests, public peptide page content checks, `npm run regulatory:review`, public smoke.

5. [ ] Sprint 5: build Product Label Analyzer v2.
   Done when: label parsing distinguishes ingredient normalization, doses, proprietary blends, duplicate/high-dose warnings, certifications, product-quality score, demo/verified status, AU/TGA status, and efficacy evidence mapping.
   Latest state: demo product signals exist; the label analyzer now parses ingredient rows with normalized names, captured amounts, metric amount normalization for g/mg/mcg labels, ingredient-specific vitamin D IU-to-mg vitamin D conversion, direct vitamin D mcg-to-mg vitamin D preservation, source-form-specific vitamin A IU-to-mg RAE conversion for retinol, supplemental beta-carotene, dietary beta-carotene, dietary alpha-carotene, and dietary beta-cryptoxanthin, source-known vitamin E IU-to-mg alpha-tocopherol conversion, and vitamin A mcg RAE, vitamin E mg alpha-tocopherol, folate mcg DFE, plus niacin mg NE equivalent-unit preservation in captured and normalized rows with provenance/caveats, non-metric unit caveats for unsupported, mixed-form, or source-unknown IU ingredients, proprietary-blend context, serving-size/per-unit/daily-use direction cues, visible AUST label identifiers, and recognized third-party certification signals as product-quality cues, computes a pasted-label product-quality score from certification, amount-transparency, proprietary-blend, duplicate-row, and non-metric-unit cues while keeping it separate from efficacy and AU/TGA status, distinguishes pasted-label-only, local demo profile match, local product profile text match, local product profile match with limited AU/TGA status, product-level AUST identifier match, stale/limited product-level AUST identifier match, intervention-only AUST context, and unmatched parsed AUST identifier states without treating any as product efficacy proof or recommendations, flags duplicate ingredient listings plus parsed serving-to-daily dose review cues, parsed Vitamin D high-amount warnings from normalized mcg/IU amounts, parsed Vitamin A upper-limit review warnings when preformed/RAE context is visible, parsed Vitamin E and Vitamin C upper-limit review warnings from metric label amounts, parsed folic-acid upper-limit review warnings only when synthetic-folate context is visible, parsed niacin upper-limit review warnings from supplemental niacin/NE amounts, parsed magnesium, zinc, selenium, calcium, iron, copper, manganese, molybdenum, phosphorus, choline, and iodine/iodide source-form upper-limit review warnings from metric label amounts while explicitly requiring elemental iodine verification for salt, kelp, or seaweed sources, and AU/TGA vitamin B6 label-warning review prompts above the 10 mg daily-dose warning threshold, with related-row totals now summed across split parsed forms while preserving form-specific warning gates for Vitamin A and folic acid, maps parsed ingredients to local intervention-level evidence rows with claim labels, review status, dose/form context, and product-level caveats, shows product AU/TGA and quality-signal filters, resolves parsed AUST identifiers against local product-level regulatory records when present while labeling intervention-only matches and unmatched identifiers as not product verification, separates verified AUST product status from captured non-positive, stale, or insufficient product-status records, adds product-profile formulation evidence maps that show local ingredient-to-claim matches while explicitly blocking product-level efficacy claims until reviewed formulation evidence exists, and includes a gated path for future human-reviewed exact-formulation product-specific efficacy rows while current seed profiles remain at 0 product-specific rows. Demo status plus quality-vs-efficacy-vs-AU/TGA separation remains visible. Remaining work: additional ingredient-specific non-metric conversions where authoritative conversions exist, more reviewed product states as real local records are added, and capturing real reviewed product-specific efficacy rows for verified product formulations.
   Validate with: parser tests, product-signal tests, UI render tests, privacy review.

6. [ ] Sprint 6: expand interventions slowly and deeply.
   Done when: the next 5-10 interventions enter through onboarding/review-kit workflows with scoped claims, citation-linked source packets, safety/regulatory caveats, and explicit review status.
   Latest state: current launch coverage is intentionally small; pending source candidates remain backlog, not public evidence. Coverage review now includes an expansion-readiness gate for the next 5-10 interventions: it keeps expansion human-owned, no-auto-promotion, and no-public-evidence-write by default, exposes read-only onboarding guide commands for single or batch draft packets, and currently blocks expansion until the 8 existing public claims receive human review of their complete source packets. The read-only coverage summary now lists the exact blocking claim IDs with intervention, packet status, priority, review status, and next action so expansion blockers are reviewable without writes.
   Validate with: `npm run onboarding:guide -- --summary`, `npm run coverage:review -- --env-file .env.vercel.preview.local --summary`, source-packet review, public smoke.

7. [ ] Add analytics, feedback, and quality instrumentation.
   Done when: privacy-safe product/operations KPIs distinguish public usage, operator workflow health, ingestion health, evidence quality, and user feedback.
   Latest state: operations monitors exist; a privacy-safe product analytics KPI framework now defines primary, driver, and guardrail metrics across public usage, operator workflow health, ingestion health, evidence quality, and user feedback. The KPI layer is definition/report-only, keeps public routes read-only, forbids user-level identifiers, raw search/label text, private health detail, IP addresses, session replay, and actor identity, and exposes `npm run analytics:kpis -- --summary`, `npm run analytics:kpis -- --aggregate-schema`, `npm run analytics:kpis -- --validate-aggregate <aggregate-json-file>`, plus related read-only monitoring links. Provider-neutral dashboard blueprints now map those KPIs into public evidence usage, operator workflow health, ingestion health, evidence quality, and feedback/boundary panels; `--aggregate-schema` prints the allowed top-level row fields, required fields, forbidden fields, metric IDs, allowed dimensions, privacy classification, and source-status contract for provider review. Aggregate export validation blocks forbidden fields, undeclared top-level payload fields, undeclared KPI dimensions, non-scalar dimension payloads, invalid period dates, reversed period ranges, unknown metrics, and malformed values before connection, while leaving public usage output blocked on a privacy-preserving aggregate analytics provider. The analytics CLI is import-safe for tests, validates JSON file exports instead of brittle inline shell JSON, and includes `--help` output that documents file-based validation plus read-only/privacy boundaries. Remaining work: choose/configure that provider and connect real aggregate outputs after review.
   Validate with: KPI definitions, privacy review, dashboard/report output, monitoring links.

## Guiding Rule

Apex Lifespan should reward boring evidence, penalize hype, and make uncertainty visible. The best version of the product is not the largest compound database; it is the one where every score is scoped, auditable, conservative, and useful.
