# Thread Handoff

Refreshed on 2026-06-13 after fully-live launch approval. Keep this file compact; do not turn it into a progress log.

## Startup

- Work in `D:\Codex\Apex Lifespan` on branch `codex/queue-claim-sources`.
- Read `AGENTS.md` and `docs/codex/project.md` first. Read this file only for resume/current state.
- Do not read archives, generated output, screenshots, or plans wholesale. Search them only for targeted history.
- Current roadmap: `docs/codex/roadmap.md` targets live operations and quality growth. Completed fully-live launch roadmap is archived at `docs/codex/archive/roadmap/2026-06-13-fully-live-end-product.md`.

## Current Repo State

- Latest local branch commit: `417c055 Clean operator QA fixture before preview seed`. Production `main` also has hotfix `68981fe Allow GitHub linking for bootstrapped operators`.
- Branch has substantial local uncommitted onboarding/launch-readiness work; review `git status -sb` before staging.
- GitHub pushing is allowed when useful, but the user explicitly said no commit/push for this cleanup unless asked.
- Default iteration mode is local-first: use `npm run dev`/localhost for rapid site tweaks, batch changes into checkpoints, use Vercel Preview only when a shareable URL or device review is useful, and use Production only after explicit stable milestones or urgent public trust/safety fixes.
- Public database-backed read-only MVP/demo is live at `https://apex-lifespan.vercel.app`.
- Branch Preview operator URL: `https://apex-lifespan-git-codex-queue-c-c7d928-tom-chanpheng-s-projects.vercel.app/operator`.

## Guardrails

- Public routes and dashboard remain read-only.
- Manual evidence/source-candidate workflows stay human-owned and explicit.
- No source-candidate auto-promotion into public evidence.
- Operator writes require authenticated operator role plus explicit write/evidence gates.
- Keep local Codex sidecar variables out of public/Vercel environments.
- Australia/TGA remains the default lens; product-level ARTG/AUST confidence needs product-level evidence.
- Avoid peptide sourcing, compounding, reconstitution, injection, cycling, dosing, or self-administration guidance.

## Deployment And Operator State

- Production public reads are intentionally in database mode after the managed DB cutover rehearsal/import; public routes remain read-only.
- Production Vercel now has database evidence, data-mode evidence, migration-rehearsal evidence, a separately generated production `AUTH_SECRET`, production `AUTH_GITHUB_ID`, production `AUTH_GITHUB_SECRET`, and `APEX_VERCEL_OPERATOR_AUTH_CONFIGURED_AT`.
- Vercel Preview has managed Neon/database/auth evidence configured through dashboard-managed envs.
- Operator GitHub/Auth.js sign-in works on Preview for `moxhelix@hotmail.com`; the account is OWNER.
- Preview browser write QA completed with a synthetic `operator-qa-*` fixture:
  - Accept, claim-link, structured extraction, promotion, and audit events were exercised.
  - Temporary write envs and `APEX_OPERATOR_QA_FIXTURE_ENABLED` were removed.
  - Cleanup redeploy on commit `417c055` removed 1 fixture candidate, 1 claim link, 1 study, 1 reference, and 3 audit events before seed integrity ran.
  - Branch Preview env now only has the durable evidence vars: `APEX_OPERATOR_FLOW_QA_REVIEWED_AT`, `APEX_OPERATOR_ACTIVE_ACCOUNT_READY`, `APEX_VERCEL_OPERATOR_AUTH_CONFIGURED_AT`, `APEX_VERCEL_DATABASE_MODE_CONFIGURED_AT`, `APEX_VERCEL_DATABASE_CONFIGURED_AT`.

## Recent Validation

- Before the fixture/order commits: focused fixture/setup tests, full `npm run test`, `npm run lint`, `npm run typecheck`, `npm run build`.
- Cleanup-order fix validation: focused setup/fixture tests, `npm run typecheck`, `npm run build`.
- Vercel cleanup deployment: branch alias Ready, `/api/health` OK, anonymous `/operator` closed, seed integrity passed.
- Live public seed-mode smoke previously passed at `https://apex-lifespan.vercel.app`.
- Refreshed 2026-06-12: production was cut over to database-backed public reads after managed DB migration/seed/review replay, and `npm run smoke:public-mvp -- https://apex-lifespan.vercel.app --require-database` passed. Production auth was later configured on 2026-06-13; current Production `/operator` shows the GitHub sign-in gate rather than auth unavailable.
- New helper path: production readiness and migration rehearsal now accept `--env-file <non-production-env-file>` so an ignored Vercel/Neon env file can be checked without pasting secrets into the shell. Current value-free `.env.vercel.preview.local` still blocks; synthetic non-production env-file probes reached sanitized ready/dry-run states without printing fake secrets.
- Non-production migration rehearsal passed on 2026-06-12T01:37:54.603Z against direct Preview Neon target `postgresql://ep-steep-unit-a722b1rw.ap-southeast-2.aws.neon.tech/neondb`: `npm run db:validate` and `npm run db:migrate:deploy` both exited 0. Operator evidence was recorded locally in `.env.vercel.preview.local`; `npm run operator:readiness -- --env-file .env.vercel.preview.local --summary` is now `17 ready / 0 blocked / 0 warnings`. Production readiness against `.env.vercel.preview.local` is now `11 ready / 0 blocked / 1 warning`. Uptime monitoring evidence is recorded locally after adding the GitHub Actions scheduled monitor on `main` at `bba47f2`; error monitoring evidence is recorded locally after adding the public API error monitor on `main` at `6a36f39`; deployment alert evidence is recorded locally after adding the Vercel commit-status monitor on `main` at `ab4a616`; scheduled ingestion alert evidence is recorded locally after adding the dry-run monitor on `main` at `7f978a9`; GitHub Actions repository execution was enabled after verification showed the workflows existed but could not run while Actions was disabled; database backup/PITR evidence is recorded locally after confirming the Vercel-connected Neon resource and 6-hour project history retention; backup restore rehearsal evidence is recorded locally after restoring Preview Neon data into temporary database `apex_restore_rehearsal_20260612062621`, validating migrations/table counts/dashboard data mode, and dropping the target; rollback drill evidence is recorded locally after identifying current and previous Ready production deployments, confirming rollback command/status, confirming data rollback path, and passing public smoke without rolling traffic; alert-test evidence is recorded locally after adding the manual-only `Launch Alert Test` workflow on `main` at `8a9f642` and verifying the post-enable run `https://github.com/Smellybum1/Apex-Lifespan/actions/runs/27399894930` completed with the expected intentional failure at `2026-06-12T06:54:22Z`; scheduled-ingestion hosted/retry evidence is recorded locally after the guarded Preview Neon apply rehearsal processed PubMed job `cmq9nhdcy000iey85ehxzdqz7` into 3 pending source candidates with automatic retries disabled and no public evidence promotion. `npm run ingest:sources` now accepts `--env-file <non-production-env-file>`; Preview source-candidate summary includes 4 succeeded source jobs, 1 accepted/human-reviewed PubMed candidate, and 14 pending unreviewed candidates. `PMID 42141930` remains pending review, has no accepted-reference match, and is not promotion-ready. Operations readiness with the env file reports `13 ready / 0 blocked`. Scheduled-ingestion dry-run with the env file reports no blocked checks, `0` queued jobs, `0` failed jobs, hosted cron ready, retry policy ready, and `noAutoPromotion=true`. Psyllium coverage now has a scoped lipid-biomarker draft claim and structured PubMed source packet in seed data and Preview Neon.
- On 2026-06-12, the user human-reviewed PubMed `28615996` for `creatine-strength`; it was accepted against curated reference `issn-creatine-2017`, promoted through the audited OWNER operator action, and recorded audit event `cmqap2n3500019ji4fohmvagc`. The user then approved all remaining complete claim packets; audited OWNER events marked `bpc-157-injury-healing`, `vitamin-d-deficiency`, `omega-3-cv-events`, `omega-3-triglycerides`, `psyllium-ldl-lipids`, `creatine-lifespan`, and `vitamin-d-longevity` human-reviewed in Preview. Latest claim-packet audit events: `cmqapmpr600019j54hokckp42`, `cmqapmqb400019jl0jyq9z1xe`, `cmqapmqvy00019jd4ifsensmw`, `cmqaq1x0a00019jacofjzl4fs`, `cmqaq1xpi00019ja0wb4km2mz`, `cmqaq1ybm00019jasyx0qqthk`, and `cmqaq1yzo00019jbs1q4qfxb6`. `npm run coverage:review -- --env-file .env.vercel.preview.local --summary` now reports 8 complete source packets, 0 intervention gaps, 8 human-reviewed claims, and 0 unreviewed claims. Aggregate launch readiness with the env file now reports `8 ready / 3 blocked / 1 warning`; evidence coverage and public database smoke are ready.
- Vercel CLI is reachable through `npx vercel` and authenticated as `smellybum1`. Vercel project/env inspection is usable for read-only evidence checks; `vercel alerts rules inspect ar_default` showed the default team alert rule, but no specific operations evidence was recorded from that alone. `vercel webhooks list` found none and `vercel crons list` found no cron jobs.
- The workflow-only `main` pushes triggered Production Vercel deployments, which reached Ready. `npm run smoke:public-mvp -- https://apex-lifespan.vercel.app` passed afterward.
- Onboarding automation pass added extraction prefill review-confidence labels and `npm run onboarding:fulltext-sources -- --next --summary` for safe full-text source inventory next-step planning. The next-step planner is now surfaced in `npm run onboarding:quality -- --summary`, `npm run onboarding:status -- --summary`, and the authenticated operator quality panel. Validation passed: focused onboarding/status/operator tests, CLI summary checks, `npm run typecheck`, `npm run lint`, `npm run test`, and `npm run build`.
- Supplement onboarding plans now include copy-safe post-draft handoff commands for seed-diff review, import-assistant preview, readiness/status, source queue preview, product review, full-text source next-step planning, DB validation, and typecheck. The same command bundle appears in Markdown output and the operator draft wizard preview. Validation passed: focused onboarding/operator tests, CLI JSON/Markdown checks, `npm run typecheck`, `npm run lint`, `npm run test`, and `npm run build`.
- Full-text source onboarding now has `npm run onboarding:fulltext-sources -- --write-review-kit docs/codex/onboarding`, which writes an approval-false local inventory starter plus the top-priority source worksheet in one preflighted step. The `--next` planner recommends this command for blank inventories, generated worksheets/connector-review packets are ignored locally, and the path still has `noAutoApproval`, `noConnectorApproval`, and `noLiveFetch`. Validation passed: focused full-text/quality/status tests, CLI write-kit and next-summary checks, `npm run typecheck`, `npm run lint`, `npm run test`, and `npm run build`.
- Full-text connector readiness now has `npm run onboarding:fulltext-sources -- --connector-plan` plus `--write-connector-plan <path>`. The plan is read-only/Markdown or summary output, routes blocked versus fixture-design-ready sources, lists fixture-first phases, planned local parser/fixture files, guardrails, validation commands, and repeats `noAutoApproval`, `noConnectorApproval`, `noLiveFetch`, and `noNetworkFetch`. The reviewed synthetic fixture reaches `ready-for-fixture-design`; default sources stay blocked. Validation passed: focused full-text/quality/status tests, connector-plan CLI smokes, `npm run typecheck`, `npm run lint`, `npm run test`, and `npm run build`.
- Full-text connector approval now has `npm run onboarding:fulltext-sources -- --connector-approval-packet` plus `--write-connector-approval-packet <path>`. The packet combines connector-review status, fixture-first plan status, dry-run support, terms/policy evidence, rate/cache/retention controls, derived-only export boundaries, planned files, guardrails, validation commands, and an operator decision template. It reports `blocked`, `hold`, or `ready-for-approval` while keeping `approvalGranted=false`, `noConnectorApproval=true`, `noImplementationApproval=true`, `noLiveFetch=true`, and `noNetworkFetch=true`. Validation passed: focused full-text source tests, blocked/ready/write CLI smokes, `npm run lint`, `npm run test`, `npm run build`, and clean rerun of `npm run typecheck`.
- `npm run onboarding:status -- --summary`, `npm run onboarding:quality -- --summary`, and the authenticated operator quality panel now surface connector-approval packet readiness counts alongside the existing source gate, inventory progress, next-step planner, and connector-review counts. The status command includes a dedicated `full-text-connector-approval` command stage, and the combined views still report `approvalGranted=false` and `noImplementationApproval=true`; no connector approval, implementation approval, live fetch, extraction write, candidate decision, or promotion is performed by these summaries.
- Full-text fixture rehearsal now has `npm run onboarding:fulltext-fixture -- --fixture-file docs/codex/onboarding/examples/fulltext-local-fixture.example.json --summary`. It parses local operator-owned fixture JSON into a derived-only report, blocks oversized excerpts, does not echo raw excerpts, and repeats no live/network fetch, no connector approval, no extraction write, no candidate decision, and no promotion. The connector-plan validation commands now include this local fixture check. Validation passed: focused fixture/source tests and CLI summary checks.
- `npm run onboarding:fulltext-fixture` now also supports `--template` and `--write-template <path>` with optional `--source-id`, `--source-url`, and `--title`. The starter is parseable but intentionally blocked until reviewed local values are filled because all derived target values are blank and confidence labels are `Missing`. Validation passed: focused fixture tests plus template/write-template CLI smokes.
- `npm run onboarding:fulltext-fixture` now supports `--extraction-draft` with optional `--candidate-key` and `--study-source-type`. It maps reviewed local fixture fields into the existing `npm run ingest:sources -- --extract-candidate-study` command shape, reports missing required write fields, keeps comparator review-only, does not echo raw excerpts, and repeats no DB validation, no extraction write, no candidate decision, no live fetch, and no promotion. Validation passed: focused fixture tests, CLI blocked-summary smoke, `npm run typecheck`, `npm run lint`, `npm run test`, and `npm run build`.
- `npm run onboarding:review-packet` packets now include a read-only `candidateReviewAutopilot` block and the summary includes `autopilotStatus`, next action, recommended candidate, and curation-draft command when available. It recommends the next candidate-review action from source-conviction ranking, but repeats `noAutoAccept`, `noAutoReject`, `noAutoExtraction`, and `noAutoPromotion`; it does not change candidate decisions, write extraction rows, mark claim reviews, or promote public evidence. Validation passed: focused review-packet test and CLI summary smoke.
- Source-conviction automation now exposes an explicit rubric and per-candidate score breakdown for source reputation, study design, traceability, recency, abstract availability, triage signal, and title/query relevance penalty. Quality/status/review-packet summaries include the rubric and keep it as decision support only; no candidate decision, extraction write, claim review, or promotion is automated by the rubric.
- `npm run onboarding:quality -- --summary`, `npm run onboarding:status -- --summary`, and the authenticated operator quality panel now surface candidate-review autopilot counts, next action, recommended candidate, curation-draft command, read-only packet/reference/sibling/status preview commands when available, and no-auto-decision flags per supplement. This makes the review autopilot visible outside raw review-packet JSON while preserving explicit candidate decisions, extraction writes, claim review, and promotion. Validation passed: focused quality/next-action/operator tests plus typecheck.
- The authenticated operator candidate-review queue now computes source-conviction autopilot metadata for pending candidates, scans a wider candidate window, sorts rows by autopilot recommendation before raw triage score, and displays recommendation, conviction score, source-reputation label, rationale, curation-draft command, read-only packet/reference/sibling/status preview commands, and no-auto-decision flags. Accept/reject controls remain behind the existing browser write gate and no queue readout changes candidate decisions. Validation passed: focused review-queue/operator tests plus typecheck.
- `npm run onboarding:seed-diff` now includes a read-only draft import plan per supplement. It distinguishes manual seed copy from future-gated database import, repeats `noAutoWrite`, `noDatabaseWrite`, `noPublicEvidenceRowsWritten`, and `noAutoPromotion`, and keeps database import unavailable until a separate authenticated/audited implementation is reviewed.
- The authenticated operator draft wizard now mirrors that import plan in the browser preview using current seed ids for collision awareness. It still only saves private `SupplementOnboardingDraft` rows behind the existing write gate and does not create seed, database intervention/claim, source-candidate, or public evidence rows.
- The authenticated operator console now lists recent private supplement drafts for admin/owner operators with read-only import-plan status, blocker/warning counts, next safe action, future-gated database-import status, and no-auto-write/no-public-evidence flags. This saved-draft review surface does not import, queue, accept, extract, mark reviewed, or promote anything.
- `npm run onboarding:review-kit -- --name <supplement>` now writes a no-overwrite local Markdown review-kit folder with index, draft packet, seed diff/import plan, handoff commands, review-packet shell, AU/TGA product-status worksheet, full-text next-step worksheet, and import-assistant worksheet. It is local-file-write only and does not edit seed data, write database rows, queue sources, decide candidates, extract studies, mark claims reviewed, approve connectors, fetch full text, or promote evidence.
- `npm run onboarding:rehearsal -- --summary` now includes a `friction` assessment with a 0-100 score, tier, itemized claim-scope/guardrail/safety/product-status/source-search/full-text-gate statuses, and next actions. Any blocked item makes the rehearsal tier `blocked`; the report remains read-only and performs no writes.
- `npm run onboarding:import-assistant -- --name <supplement> --summary` now previews the manual seed-copy path and future database-import shape from seed-diff/import-plan logic. It lists planned draft `Intervention`, `Claim`, and `AustraliaRegulatoryStatus` records, required future gates, public-promotion prerequisites, and no-write/no-promotion flags while keeping database import `future-gated`, unsupported, and commandless. The local review kit now writes `07-import-assistant.md`.
- The authenticated operator console now shows a guided onboarding workflow panel that combines saved private drafts, source/candidate next-action routing, promotion readiness, source-tracking gates, and full-text source gates into one ordered read-only "what next" list. It previews commands only and performs no seed/database write, candidate decision, extraction, claim review, connector approval, full-text fetch, or promotion.
- `npm run onboarding:monitor -- --summary`, `npm run onboarding:quality -- --summary`, `npm run onboarding:status -- --summary`, and the authenticated operator console now include a read-only post-onboarding monitor for stale review evidence, risk-aware refresh cadence, missing/Unknown AU/TGA product-status records, pending candidates, extraction gaps, unreviewed claims, safety-signal caveats, promotion-ready packets, source-tracking gaps, and full-text source-gate issues. Refresh policy is explainable per supplement: routine 365-day checks, 180-day watch checks, and 90-day high-attention checks for peptide/watchlist, safety, regulatory, or clinician-sensitive contexts. The monitor emits next-command previews only and performs no seed/database write, candidate decision, extraction, claim review, connector approval, full-text fetch, or promotion.
- Saved supplement drafts now have a separately gated `onboarding:import` operator browser action for database import. It requires the normal operator write/browser evidence plus `APEX_ONBOARDING_DATABASE_IMPORT_ENABLED=true` and `APEX_ONBOARDING_DATABASE_IMPORT_REVIEWED_AT`, requires an import note, blocks unreadable/duplicate/unsafe drafts, creates only draft `Intervention`, unreviewed `Claim`, and Unknown AU/TGA placeholder rows, marks the draft `READY_FOR_REVIEW`, and records `supplementOnboarding.databaseImport`. It does not accept/reject candidates, link references, extract studies, mark claims human-reviewed, approve connectors, fetch full text, infer AU/TGA product status, or promote evidence. Validation passed: focused operator import/control/page tests, `npm run typecheck`, `npm run lint`, `npm run test`, and `npm run build`.
- `npm run onboarding:packet` now provides the one-command onboarding packet. Draft mode (`--name ...`) combines draft plan, seed-diff/import-assistant summary, review-kit file list, explicit post-review source queue command, and full-text next action. Existing mode (`--supplement ...`) combines status, readiness, review-packet/source-conviction summary, candidate-review autopilot, full-text gates, monitor, and next commands. The command is read-only and labels follow-up commands as read-only, local-file-write, or explicit-write-after-review; it does not write seed/database rows, queue sources, decide candidates, extract studies, approve connectors, fetch full text, mark reviews, or promote evidence. Validation passed: focused packet test and draft/existing CLI summary smokes.
- Batch packet orchestration is now covered by the same command. Draft batches use `npm run onboarding:packet -- --batch-file docs/codex/onboarding/examples/batch-onboarding.json --summary`; existing supplement batches use `npm run onboarding:packet -- --supplements creatine,vitamin-d --summary`. Batch output keeps each supplement's packet summary, status, next action, and commands isolated, then adds a small ready/warning/blocked rollup and supplement-prefixed command previews. Validation passed: focused packet tests plus draft-batch and existing-batch CLI summary smokes.
- Batch review-kit writing now creates a local `batch-review-kit-index.md` beside the per-supplement kits when running `npm run onboard:supplement -- --batch-file docs/codex/onboarding/examples/batch-onboarding.json --write-review-kit`. The writer preflights the batch index and all kit files before writing to avoid partial local output, and the index preserves local-file-write-only/no-auto-write/no-database-write/no-candidate-decision/no-extraction-write/no-auto-review/no-auto-promotion boundaries. Validation passed: batch review-kit CLI smoke in `output/onboarding-batch-review-kit-smoke`, targeted onboarding review-kit tests, typecheck, lint, full test, build, and diff check.
- The operator guided onboarding workflow now includes a `Batch review kit` step when several private drafts or visible onboarding supplements can be coordinated together. It shows `npm run onboard:supplement -- --batch-file <reviewed-batch-json> --write-review-kit`, includes an existing-supplement batch packet preview command when slugs are visible, and remains read-only/browser-safe with no seed/database write, candidate decision, extraction, review, or promotion. Validation passed: guided-onboarding and operator page tests plus typecheck.
- Full-text source review kits now support `--all-sources`: `npm run onboarding:fulltext-sources -- --write-review-kit <dir> --all-sources` writes an approval-false inventory starter, `fulltext-source-review-kit-index.md`, and one worksheet per ranked full-text source class. It refuses `--source-id`, reports source worksheet count, and preserves no-auto-approval/no-connector-approval/no-live-fetch/no-source-candidate/no-extraction/no-claim-review/no-promotion boundaries. Validation passed: focused full-text source test, CLI smoke in `output/fulltext-all-sources-review-kit-smoke`, typecheck, lint, full test, build, and diff check.
- Full-text next-action routing now recommends the all-source review kit first for blank inventories via `npm run onboarding:fulltext-sources -- --next --summary`; the focused single-source kit remains as the narrower second local-file-write command. This keeps source review setup low-friction while preserving no approval, no connector approval, no live fetch, no extraction write, no claim review, and no promotion boundaries.
- Full-text connector prep now has a bundled local writer: `npm run onboarding:fulltext-sources -- --inventory-file <reviewed-local-inventory> --write-connector-prep-kit docs/codex/onboarding`. It preflights and writes an index plus connector-review, fixture-first plan, and approval-packet Markdown files while preserving `approvalGranted=false`, `noConnectorApproval=true`, `noImplementationApproval=true`, `noLiveFetch=true`, and `noNetworkFetch=true`.
- `npm run onboarding:status -- --fulltext-inventory-file <reviewed-local-inventory> --summary` now surfaces that bundled connector prep writer as `full-text-connector-prep-kit` when an inventory path is present. Default status summaries still route blank inventories to the all-source review kit first.
- `npm run onboarding:packet -- --supplement <id-or-slug> --fulltext-inventory-file <reviewed-local-inventory> --summary` now also flattens the planner-recommended `write-connector-prep-kit` command into the packet command list when the inventory is connector-review-ready.
- The connector prep kit also writes blank `fulltext-fixtures/<connector-key>.local.json` fixture starters for fixture-design-ready sources. They contain placeholder excerpt text only and remain blocked until reviewed local values are filled.
- Full-text next-action, status, and packet summaries now surface read-only fixture-starter follow-ups after connector prep: validate the generated `fulltext-fixtures/<connector-key>.local.json` starter and preview an extraction draft. These previews do not write extraction rows, decide candidates, mark claim reviews, or promote evidence.
- `npm run onboarding:fulltext-fixture -- --fixture-dir docs/codex/onboarding/fulltext-fixtures --progress --summary` now provides a read-only fixture progress rollup with blank, filled, extraction-draft-ready, blocked, missing, unparseable, and parseable counts. The same progress command is surfaced in full-text next-action, status, and packet summaries.
- The authenticated operator onboarding quality panel and guided workflow snapshot now surface fixture follow-up command previews when full-text source next action reaches connector prep: fixture progress, local validation, and extraction-draft preview. These remain command previews only with no connector approval, live fetch, candidate decision, extraction write, claim review, or promotion.
- `npm run onboarding:guide -- --name <supplement> ... --summary` and `npm run onboarding:guide -- --supplement <id-or-slug> ... --summary` now provide a concise happy-path readout on top of the packet builders. The guide recommends one safest next command, keeps the command queue, labels read-only/local-file-write/explicit-write-after-review modes, and performs no writes or approvals.
- `npm run onboarding:guide -- --batch-file docs/codex/onboarding/examples/batch-onboarding.json --summary` and `npm run onboarding:guide -- --supplements creatine,vitamin-d --summary` now provide the same happy-path readout for batch packet modes. Batch guide output keeps supplement-prefixed command labels and isolated packet context, and existing-supplement batches can surface connector-prep/fixture follow-up commands when a reviewed full-text inventory file is supplied.
- Existing-supplement packet/guide output now surfaces the blank-inventory all-source full-text review-kit command and inventory progress follow-ups directly. `npm run onboarding:guide -- --supplement creatine --summary` recommends `Create all-source review kit` as a local-file-write step when no full-text source inventory has started, without approving a source/connector, fetching full text, writing extraction rows, marking reviews, or promoting evidence.
- Existing-supplement batch packet/guide output now de-duplicates shared full-text source setup commands. Supplement-specific commands stay prefixed per supplement, but global all-source review-kit, inventory progress, connector-prep, and fixture commands appear once because they operate on the shared local source inventory.
- Connector-review-ready packet/guide output now also surfaces connector readiness, connector review packet, fixture-first implementation plan, and connector approval packet preview/write commands. These are read-only or local-file-write only and do not grant source approval, connector approval, implementation approval, live fetch, extraction writes, claim review, or promotion.
- Compact connector approval summaries now include each row's operator decision template and validation commands while still reporting `approvalGranted=false`, `noConnectorApproval=true`, `noImplementationApproval=true`, and no live/network fetch.
- Connector review, implementation-plan, approval-packet, and prep-kit summaries now thread the supplied `--inventory-file` path into generated validation commands instead of falling back to the default local inventory path.
- Connector approval summaries now include full checklist item ids, labels, completion flags, and details, while keeping incomplete-item labels and `approvalGranted=false`.
- On 2026-06-13, the user explicitly approved the separate full-text connector for fixture-only implementation review. Evidence is recorded in `docs/codex/onboarding/fulltext-connector-approval-decision.md`; live network/full-text capture remains gated and unapproved.
- Production step 3 is complete: Vercel Production env list confirms database variables/evidence, data-mode evidence, migration-rehearsal evidence, `AUTH_SECRET`, `AUTH_GITHUB_ID`, `AUTH_GITHUB_SECRET`, and `APEX_VERCEL_OPERATOR_AUTH_CONFIGURED_AT`. Production was redeployed as `https://apex-lifespan-jgcomlj13-tom-chanpheng-s-projects.vercel.app` and aliased to `https://apex-lifespan.vercel.app`; strict database public smoke passed and anonymous `/operator` shows the GitHub sign-in gate. `npm run production:readiness -- --env-file .env.vercel.production.local --summary` reports `10 ready / 0 blocked / 2 warnings`.
- Production step 6 is complete: `moxhelix@hotmail.com` is an active OWNER operator in the managed database via audited bootstrap event `cmqblk1im00039ja8wdjf9bxi`; Production Vercel has active-operator, non-production write QA, manual operator-flow QA, and browser-write-control approval evidence recorded. Production was redeployed again as `https://apex-lifespan-d1go0sdnl-tom-chanpheng-s-projects.vercel.app`; strict public database smoke and `npm run operator:smoke -- https://apex-lifespan.vercel.app --expect-auth-required` passed. `npm run operator:readiness -- --env-file .env.vercel.production.local --summary` reports `17 ready / 0 blocked / 0 warnings`. Production writes remain disabled by default.
- Roadmap step 7 is complete: Preview DB curation status for PubMed `28615996` is `Public source packet ready` with decision `Accepted`, review status `Human reviewed`, accepted reference `issn-creatine-2017`, claim links, structured extraction `study-creatine-issn`, and `publicSourcePacketReady=true`. Promotion readiness with `.env.vercel.preview.local` reports `0 blocked / 1 ready`, targeted promotion/browser-control tests passed, and launch readiness marks `promotion-readiness` ready.
- Roadmap step 12 is complete locally: `npm audit` was fixed by updating transitive `esbuild` to `0.28.1`; full tests, lint, typecheck, build, DB validation, audit, live Production strict public database smoke, exact security-header assertion, and local production database-mode browser QA passed. Dashboard mobile overflow and search/filter label association were fixed in `src/components/evidence-dashboard.tsx`. Browser artifacts: `output/playwright/step12-local-db-desktop.png` and `output/playwright/step12-local-db-mobile.png`.
- Launch step 13 evidence friction was reduced with `npm run launch:evidence`. It dry-runs by default, writes only to untracked in-workspace env files with `--write`, refuses silent overwrite unless `--force`, records admin-flow smoke/post-launch-review/final-approval notes, and performs no database or production write. The launch-readiness worksheet now includes dry-run evidence commands for those final three gates. Validation passed: focused launch-evidence/readiness/env-file tests, dry-run CLI smokes for all three evidence types, `npm run typecheck`, and `npm run lint`. Launch readiness remains `8 ready / 3 blocked / 1 warning` until authenticated production admin smoke, final approval, and post-launch review scheduling are real.
- Production operator sign-in initially hit Auth.js account linking error text: "To confirm your identity, sign in with the same account you used originally." Root cause was the pre-bootstrapped operator `User` row needing to attach its first GitHub OAuth account. Clean production hotfix commit `68981fe` on `main` changed `src/auth.ts` to allow GitHub email account linking for bootstrapped operators and included the `esbuild` audit lockfile bump. Production redeployed as `https://apex-lifespan-jq6b5yqir-tom-chanpheng-s-projects.vercel.app`; alias `https://apex-lifespan.vercel.app` points there. Post-deploy validation: clean worktree `npm run typecheck`, `npm run lint`, `npm audit`, `npm run build`; live anonymous `npm run operator:smoke -- https://apex-lifespan.vercel.app --expect-auth-required`; and current-worktree strict `npm run smoke:public-mvp -- https://apex-lifespan.vercel.app --require-database`.
- The user then confirmed "Production admin smoke passed." `npm run launch:evidence -- --env-file .env.vercel.preview.local --evidence admin-flow-smoke --url https://apex-lifespan.vercel.app/operator --note ... --write --summary` recorded `APEX_ADMIN_FLOW_SMOKE_PASSED_AT` plus note/url locally with no database or production write. The user then scheduled the post-launch review for Sunday, June 14, 2026 at 2:00 PM Australia/Brisbane and explicitly approved the fully-live launch. `npm run launch:evidence` recorded post-launch-review and final-approval evidence locally. Refreshed launch readiness is `11 ready / 0 blocked / 1 warning`, overall `ready`; live public database smoke, anonymous operator smoke, and operations readiness passed. The completed fully-live roadmap was archived at `docs/codex/archive/roadmap/2026-06-13-fully-live-end-product.md`, and `docs/codex/roadmap.md` now tracks live operations and quality growth.
- Production `main` now includes the public trust-label patch `aa9a632 Add public prototype dataset notice`, plus smoke-helper fix `d0c9135 Update public smoke for reviewed launch data`. Live deployment `https://apex-lifespan-ltmcwd5j4-tom-chanpheng-s-projects.vercel.app` is Ready and aliased to `https://apex-lifespan.vercel.app`. Verified live: prototype/seed-dataset notice present, strict database public smoke passed, and anonymous operator smoke passed.

## Open Work

- Fully-live launch is complete and archived; public URL remains `https://apex-lifespan.vercel.app`.
- Next roadmap step: complete the scheduled 24-48 hour post-launch review on Sunday, June 14, 2026 at 2:00 PM Australia/Brisbane and record issues/decisions in `docs/codex/post-launch-review-template.md`.
- Stabilization follow-up: review and organize the local uncommitted onboarding, launch-evidence, dashboard QA, and roadmap changes into intentional commits or PRs without staging unrelated work.
- Pending source candidates remain human-review backlog and are not auto-promoted.

## Useful Commands

- Status: `git status -sb`, `git log --oneline -5 --decorate`.
- Public demo smoke: `npm run smoke:public-mvp -- https://apex-lifespan.vercel.app`.
- Fully-live smoke gate: `npm run smoke:public-mvp -- <fully-live-url> --require-database`.
- Non-production env-file rehearsal: `npm run production:readiness -- --env-file <non-production-env-file> --summary`, then `npm run production:migration-rehearsal -- --env-file <non-production-env-file>`, then explicit reviewed `npm run production:migration-rehearsal -- --env-file <non-production-env-file> --apply`.
- Env-file readiness summaries: `npm run operator:readiness -- --env-file <non-production-env-file> --summary`, `npm run operations:readiness -- --env-file <non-production-env-file> --summary`, `npm run coverage:review -- --env-file <non-production-env-file> --summary`, `npm run promotion:readiness -- --env-file <non-production-env-file> --summary`, `npm run launch:readiness -- --env-file <non-production-env-file> --summary`.
- Source-candidate Preview inspection: `npm run ingest:sources -- --env-file <non-production-env-file> --summary`, `npm run ingest:sources -- --env-file <non-production-env-file> --candidate-review-overview --candidate-review-overview-limit 10`.
- Readiness summaries: `npm run launch:readiness -- --summary`, `npm run production:readiness -- --summary`, `npm run operator:readiness -- --summary`, `npm run operations:readiness -- --summary`, `npm run coverage:review -- --summary`.
- Onboarding next-step summary: `npm run onboarding:fulltext-sources -- --next --summary`; include `--inventory-file <user-owned-local-inventory>` after creating one. Post-onboarding monitor summary: `npm run onboarding:monitor -- --summary` or `npm run onboarding:monitor -- --supplement <supplement-id-or-slug> --summary`.
- Validation: `npm run test`, `npm run lint`, `npm run typecheck`, `npm run build`, `npm audit`.
