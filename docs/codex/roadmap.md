# Roadmap: Fully Live End Product

Last updated: 2026-06-11

## Target

Ship Apex Lifespan as a fully live, public evidence intelligence product with managed production data, authenticated human review, scheduled source ingestion, explicit curation promotion, product-level AU/TGA verification workflows, monitoring, backups, privacy/terms, and launch operations.

The public seed-backed MVP/demo is already live at `https://apex-lifespan.vercel.app`. This roadmap starts from that baseline and works toward the production-grade version.

## Non-Goals

- Automated promotion of source candidates into public evidence without human review.
- Public unauthenticated review/admin writes.
- Medical advice, diagnosis, treatment instructions, peptide sourcing, compounding, reconstitution, injection, cycling, dosing, or self-administration guidance.
- Product-level ARTG/AUST confidence inferred from generic intervention evidence.

## Current Baseline

- Public MVP/demo URL: `https://apex-lifespan.vercel.app`.
- Public data mode: `APEX_DATA_SOURCE=seed`.
- Public routes and dashboard are read-only.
- Live PubMed and ClinicalTrials.gov previews are user-triggered unreviewed research leads.
- Source-candidate ingestion/review/promotion remains local and human-owned.
- Completed public-MVP roadmap archive: `docs/codex/archive/roadmap/2026-06-11-public-live-mvp.md`.

## Ordered Steps

1. [x] Reconfirm the live baseline and repo state.
   Done when: public smoke still passes, the production branch/commit is known, and the local worktree is clean before new fully-live work starts.
   Validate with: `git status -sb`, `git log -3 --oneline`, `npm run smoke:public-mvp -- https://apex-lifespan.vercel.app`.
   Completed 2026-06-11: `git status -sb` reported a clean `codex/queue-claim-sources...origin/codex/queue-claim-sources`; `git log -3 --oneline` reported `30d6830 Record public MVP launch`, `762f5cb Generate Prisma client during build`, and `9632d8f Record restored GitHub deploy path`; `npm run smoke:public-mvp -- https://apex-lifespan.vercel.app` passed homepage caveats, PubMed live preview, ClinicalTrials.gov live preview, and invalid-term guards.

2. [x] Decide the production data architecture.
   Done when: the managed PostgreSQL provider, region, environment variable model, migration policy, backup expectations, and rollback expectations are documented.
   Validate with: docs diff review and an explicit decision note in this roadmap or handoff.
   Completed 2026-06-11: selected Neon Postgres through the Vercel Marketplace, initial region `aws-us-east-1`, Vercel Preview/Production-scoped `DATABASE_URL` plus `APEX_DATA_SOURCE=database`, Prisma `migrate deploy` rehearsal before production, Neon point-in-time restore plus exported backup rehearsal, and app/data rollback expectations. Decision note: `docs/codex/production-data-architecture.md`.

3. [ ] Provision production database and secrets.
   Done when: production/staging database URLs are configured in the hosting environment, secrets are not committed, Prisma can connect, and seed fallback is not accidentally masking production database failures.
   Validate with: `npm run db:validate`, `npm run db:migrate:deploy` against a non-production rehearsal database, and a production readiness checklist before any production migration.
   Blocked 2026-06-11: requires operator access to Vercel/Neon console and secret entry. Local evidence: `.vercel` is not linked, Vercel CLI is not available on PATH, and `npm run db:validate` passed against the committed Prisma schema. Continue once Neon production/staging targets and Vercel environment variables can be configured.
   In progress 2026-06-11: added `docs/codex/plans/2026-06-11-production-readiness-report.md`, sanitized readiness helper tests, and `npm run production:readiness`. Current local report is read-only and blocked as expected: missing managed `DATABASE_URL`, missing `APEX_DATA_SOURCE=database`, no `.vercel/project.json`, missing operator auth secrets, plus warnings for Vercel CLI and NCBI metadata. It confirms committed migrations are present, no private env files are tracked, no local Codex sidecar secrets are configured, and operator writes are not enabled. Validation passed: `npm run test -- src/lib/production-readiness.test.ts`, `npm run production:readiness`, `npm run db:validate`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
   In progress 2026-06-11: added `docs/codex/plans/2026-06-11-production-migration-rehearsal.md` and dry-run-default `npm run production:migration-rehearsal`. The helper requires a managed non-local `DATABASE_URL`, `APEX_DATA_SOURCE=database`, committed migrations, `APEX_MIGRATION_REHEARSAL_TARGET=non-production`, and `--apply` before running `npm run db:validate` then `npm run db:migrate:deploy`; it refuses production targets and does not print secret URL values. `npm run production:readiness` now also blocks until `APEX_MIGRATION_REHEARSAL_PASSED_AT` is recorded after a reviewed non-production rehearsal. Current local dry run is blocked as expected for missing database URL, database mode, and rehearsal target; `npm run production:migration-rehearsal -- --apply` fails closed with no command results in the current local environment. Validation passed: migration-rehearsal/production-readiness/launch-readiness tests, `npm run db:validate`, `npm run production:migration-rehearsal`, fail-closed `npm run production:migration-rehearsal -- --apply`, `npm run production:readiness`, `npm run launch:readiness`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
   In progress 2026-06-11: added a read-only production provisioning worksheet to `npm run production:readiness`. The report now groups blocked checks, warnings, ready checks, and the next operator-owned action without printing secret values. Current local report remains blocked with 5 blockers (`DATABASE_URL`, `APEX_DATA_SOURCE=database`, migration rehearsal evidence, Vercel project link, operator auth secrets), 2 warnings (Vercel CLI and NCBI metadata), and 4 ready checks; the first next action is configuring a managed Neon/Vercel `DATABASE_URL`. Validation passed: production/launch-readiness tests, `npm run production:readiness`, `npm run launch:readiness`, `npm run db:validate`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
   In progress 2026-06-11: added `docs/codex/production-provisioning-checklist.md` as the operator-owned Neon/Vercel setup checklist and made `npm run production:readiness` verify it as a local artifact. Current local report remains blocked on the same 5 external checks, has 2 warnings, and now has 5 ready checks including the checklist; `npm run launch:readiness` still surfaces production data/secrets as the first launch blocker. Validation passed: full `npm run test`, production/launch-readiness tests, `npm run production:readiness`, `npm run launch:readiness`, `npm run db:validate`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
   In progress 2026-06-11: expanded `docs/codex/production-provisioning-checklist.md` with a value-free Vercel environment packet naming Preview/staging and Production variables, the sidecar variables that must not be added to Vercel, and evidence variables that must not be recorded until their matching checklist item is complete. Current production readiness remains blocked on external database/secrets setup. Validation passed: `npm run production:readiness`, `npm run launch:readiness`, `npm run db:validate`, and `git diff --check`.
   In progress 2026-06-11: relaxed the Vercel project readiness guardrail for the actual GitHub-import deployment path. `npm run production:readiness` now accepts either a local `.vercel/project.json` link or reviewed `APEX_VERCEL_PROJECT_CONFIGURED_AT` evidence after confirming the Vercel dashboard project, repository, branch, and production domain; the local report still blocks on managed `DATABASE_URL`, `APEX_DATA_SOURCE=database`, migration rehearsal evidence, Vercel project evidence, and operator auth secrets until those external values are configured. A temporary-env probe with `APEX_VERCEL_PROJECT_CONFIGURED_AT` proved only the Vercel project blocker drops while database/secrets gates remain blocked. Validation passed: production-readiness/env-template tests, `npm run production:readiness`, temporary-env `npm run production:readiness`, and `npm run launch:readiness`.
   In progress 2026-06-11: clarified the staging-first Vercel/Neon dashboard sequence in `docs/codex/production-provisioning-checklist.md` after the seed-backed public demo was confirmed working. The checklist now keeps Production seed mode during provisioning, creates Preview/staging database mode first, runs non-production migration rehearsal before production cutover, and delays Production `APEX_DATA_SOURCE=database` until backups, restore rehearsal, rollback drill, and database-mode QA are ready. Validation passed: `npm run production:readiness`, `npm run launch:readiness`, `npm run db:validate`, and `git diff --check`.
   In progress 2026-06-11: added `worksheet.copySafeCommands` to `npm run production:readiness` so the blocked local report emits the exact copy-safe commands for refreshing readiness, planning migration rehearsal, explicitly applying non-production rehearsal, validating Prisma, and refreshing launch readiness without printing secret values. The apply command is labeled `explicit-apply` and remains gated by the existing non-production migration rehearsal checks. Validation passed: `npm run test -- src/lib/launch-readiness.test.ts src/lib/production-readiness.test.ts`, `npm run production:readiness`, `npm run launch:readiness`, `npm run db:validate`, `npm run lint`, `npm run typecheck`, `npm run build`, and `git diff --check`.
   In progress 2026-06-11: `npm run production:readiness` now accepts value-free Vercel dashboard evidence for managed `DATABASE_URL`, Vercel `APEX_DATA_SOURCE=database`, and Vercel operator auth secrets through `APEX_VERCEL_DATABASE_CONFIGURED_AT`, `APEX_VERCEL_DATABASE_MODE_CONFIGURED_AT`, and `APEX_VERCEL_OPERATOR_AUTH_CONFIGURED_AT`, without requiring secret values in the local shell. `npm run launch:readiness` now snapshots `process.env` before data helpers can load local `.env`, so the aggregate next action follows the stable evidence snapshot instead of local Docker state. Validation passed: `npm run test -- src/lib/production-readiness.test.ts src/lib/env-example.test.ts src/lib/launch-readiness.test.ts`, `npm run production:readiness`, `npm run launch:readiness`, `npm run db:validate`, `npm run lint`, `npm run typecheck`, `npm run build`, and `git diff --check`.
   In progress 2026-06-11: added focused read-only production readiness packets via `npm run production:readiness -- --check <check-id>` and surfaced the placeholder command in the production worksheet. The focused packet prints one production check with its status, evidence keys, available ids, and next action; missing ids exit 1 with a not-found packet. Current local report remains blocked on 5 real external checks (`DATABASE_URL`, `APEX_DATA_SOURCE=database`, migration rehearsal evidence, Vercel project evidence, and operator auth secrets), with 5 ready checks and 2 warnings. Validation passed: production/launch/env tests, `npm run production:readiness`, `npm run production:readiness -- --check database-url`, expected fail-closed `npm run production:readiness -- --check missing-check`, expected bad-argument failure, `npm run launch:readiness`, `npm run db:validate`, `npm run dev:stop`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.

4. [ ] Migrate from seed-backed demo to database-backed public reads.
   Done when: public deployment intentionally uses `APEX_DATA_SOURCE=database`, the dashboard renders from managed data, and public failure modes stay safe if the database is unreachable.
   Validate with: `npm run test`, `npm run typecheck`, `npm run build`, public smoke, and a database-mode browser review.
   Blocked 2026-06-11: depends on step 3 provisioning managed database URLs and setting `APEX_DATA_SOURCE=database` in the relevant Vercel environment.
   In progress 2026-06-11: tightened strict database mode so `APEX_DATA_SOURCE=database` now fails closed when PostgreSQL is reachable but has not been seeded with dashboard interventions and claims, instead of silently rendering seed fallback data. Local/auto mode still uses the seed fallback for development convenience. Production database-mode browser review remains blocked on managed database provisioning. Validation passed: dashboard data-source tests, full `npm run test`, `npm run production:readiness`, `npm run launch:readiness`, `npm run db:validate`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
   In progress 2026-06-11: added a sanitized public unavailable state for homepage dashboard data-load failures. Strict database-mode failures now render a read-only message with privacy/terms links and no seed fallback or raw database error details. Production database-mode browser review remains blocked on managed database provisioning. Validation passed: `npm run test -- src/app/page.test.tsx`, full `npm run test`, `npm run launch:readiness`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.

5. [x] Design authenticated operator/admin workflow.
   Done when: auth provider, roles, protected routes, audit fields, local-vs-public write boundaries, and emergency disable/rollback behavior are agreed before implementation.
   Validate with: architecture note or plan plus tests proving public routes remain read-only.
   Completed 2026-06-11: selected Auth.js with GitHub OAuth, Prisma-backed sessions, explicit operator roles, protected `/operator` or `/admin` surfaces, audited `/api/operator/*` writes, public route separation, `APEX_OPERATOR_WRITES_ENABLED` emergency disable, and Vercel/Neon rollback split. Design note: `docs/codex/operator-admin-auth-design.md`. Validation: `npm run test -- src/app/api/live-source-readonly-boundary.test.ts src/app/api/source-search-routes.test.ts src/components/evidence-dashboard-live-preview-boundary.test.ts`.

6. [ ] Build authenticated review/admin surfaces.
   Done when: source-candidate review, accept/reject, claim linking, extraction, and promotion controls are available only to authenticated operators and every write is explicit and auditable.
   Validate with: route authorization tests, write-boundary tests, and manual operator-flow QA.
   In progress 2026-06-11: added `docs/codex/plans/2026-06-11-authenticated-operator-surfaces.md`, Auth.js-compatible Prisma models, operator role/status models, and append-only operator audit schema foundation. Local migration `20260611131000_operator_auth_foundation` applied successfully with `npm run db:migrate`; `npm run db:validate`, `npm run db:generate`, public read-only boundary tests, and `npm run typecheck` passed.
   In progress 2026-06-11: added tested operator authorization policy helpers in `src/lib/operator/authorization.ts`; writes fail closed unless `APEX_OPERATOR_WRITES_ENABLED=true`, disabled operators are rejected, and role gates cover audit, candidate review, curation, promotion, and operator management. Validation: `npm run test -- src/lib/operator/authorization.test.ts src/app/api/live-source-readonly-boundary.test.ts src/app/api/source-search-routes.test.ts src/components/evidence-dashboard-live-preview-boundary.test.ts`; `npm run typecheck`.
   In progress 2026-06-11: added Auth.js/Prisma adapter dependencies, fail-closed auth configuration, `/operator` shell, and auth environment checks. Without `DATABASE_URL`, `AUTH_SECRET`, and GitHub OAuth env vars, the operator route renders an unavailable state instead of querying protected data. Review queue and mutating controls are still intentionally not exposed. Validation: operator tests plus public-boundary tests passed, `npm run typecheck`, `npm run build`, `npm run lint`, and `npm audit` passed.
   In progress 2026-06-11: added a read-only operator review queue adapter and `/operator` queue panel for active reviewer+ roles. No mutating controls or operator API writes were added. Validation: `npm run test -- src/lib/operator/authorization.test.ts src/lib/operator/session.test.ts src/lib/operator/review-queue.test.ts src/app/api/live-source-readonly-boundary.test.ts src/app/api/source-search-routes.test.ts src/components/evidence-dashboard-live-preview-boundary.test.ts`, `npm run typecheck`, `npm run build`, `npm run lint`, and `npm audit` passed; an earlier parallel typecheck/build race was rerun sequentially and passed.
   In progress 2026-06-11: added audited operator server-action wrappers for candidate accept/reject, claim linking, and study extraction. Wrappers require role permission, fail closed unless writes are enabled, call the existing human-owned source-candidate helpers, and append `OperatorAuditEvent` records. No public mutating routes or browser write controls were added. Validation: `npm run test -- src/lib/operator/source-candidate-actions.test.ts`, targeted operator/public-boundary test suite, `npm run typecheck`, `npm run build`, and `npm audit` passed.
   In progress 2026-06-11: added `docs/codex/plans/2026-06-11-operator-bootstrap.md` and `npm run operator:bootstrap`, a local-only operator account bootstrap helper. It is dry-run by default and writes only with `--apply`, matching `--confirm-email`, and `APEX_OPERATOR_WRITES_ENABLED=true`; applied writes upsert `User`/`OperatorProfile` and append an `OperatorAuditEvent`. Dry run for `owner@example.com` passed without DB writes. Validation passed: bootstrap tests, targeted operator/public-boundary tests, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
   In progress 2026-06-11: added `docs/codex/plans/2026-06-11-auth-route-mount.md` and mounted Auth.js handlers at `/api/auth/[...nextauth]` so configured GitHub OAuth can complete operator sessions. The auth route is covered by a dedicated boundary test and the live-source public-route scan now excludes only that exact auth route while still forbidding source-candidate persistence from public routes. Validation passed: auth-route/public-boundary/operator-session tests, broader operator/public-boundary bundle, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
   In progress 2026-06-11: added `docs/codex/plans/2026-06-11-operator-auth-controls.md` and explicit `/operator` GitHub sign-in/sign-out server-action forms backed by the mounted Auth.js route. Auth-unavailable deployments still fail closed, disabled operators can sign out, active operators can sign out, and the candidate review queue remains read-only with no browser accept/reject, claim-link, extraction, or promotion controls. Validation passed: operator auth-control tests, auth-route/public-boundary/operator-session tests, broader operator/public-boundary bundle, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
   In progress 2026-06-11: added sanitized read-only `npm run operator:readiness` and unit coverage for ready, blocked, missing-local-artifact, and current-write-gate warning states. Current local report has 8 ready checks for the operator page, auth route, read-only queue, audited action wrappers, promotion readiness snapshot, bootstrap script, manual QA checklist, and disabled current write gate; it remains blocked on database-backed GitHub auth variables, active-operator evidence, non-production write QA, manual operator-flow QA, and browser-write-control approval. Validation passed: operator readiness tests, `npm run operator:readiness`, targeted operator/public-boundary tests, full `npm run test` (38 files, 390 tests), `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
   In progress 2026-06-11: added `docs/codex/operator-manual-qa-checklist.md` for non-production operator sign-in/access, read-only surface, controlled write, audit, promotion-blocker, emergency-disable, and evidence-variable checks before browser write controls are approved. `npm run operator:readiness` now verifies the checklist exists and points manual-QA blockers to it. Validation passed: operator readiness tests, `npm run operator:readiness`, targeted operator/public-boundary tests, `npm run typecheck`, `npm run lint`, `npm audit`, and `git diff --check`.
   In progress 2026-06-11: added a read-only operator readiness worksheet to `npm run operator:readiness`. The report now groups ready local artifacts, ready evidence, blockers, warnings, and the next operator-owned action without printing secret values or exposing browser write controls. Current local report has 7 ready local artifacts, 1 ready evidence item (operator writes disabled), and 5 blockers: database-backed GitHub auth, active operator evidence, non-production write QA, manual operator-flow QA, and browser-write-control approval. Validation passed: operator/launch-readiness tests, targeted operator/public-boundary tests, `npm run operator:readiness`, `npm run launch:readiness`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
   In progress 2026-06-11: added approval-gated browser write controls for candidate accept/reject, claim linking, and study extraction. Controls render only when an active operator has permission, `APEX_OPERATOR_WRITES_ENABLED=true`, non-production write QA evidence, manual flow QA evidence, and `APEX_OPERATOR_BROWSER_WRITE_CONTROLS_APPROVED_AT`; the server action parsers fail closed before calling audited wrappers when approval is missing. Current local `npm run operator:readiness` has 10 ready local checks and still blocks on database-backed GitHub auth, active operator evidence, non-production write QA, manual operator-flow QA, and browser-write-control approval. Validation passed: browser-control/action tests, operator readiness tests, audited wrapper tests, auth-route/public-boundary tests, and `npm run operator:readiness`.
   In progress 2026-06-11: added `npm run operator:smoke -- <base-url>` as a focused anonymous `/operator` boundary smoke. The helper verifies security headers, requires a closed auth-unavailable or auth-required state, and fails if review queue, promotion, sign-out, or write-control text is visible to anonymous users. `npm run operator:readiness` now verifies the smoke helper as a ready local artifact; current local report has 11 ready checks and the same 5 external/manual QA blockers. Validation passed: full `npm run test`, operator-smoke/operator-readiness/public-smoke tests, `npm run operator:readiness`, `npm run launch:readiness`, `npm run operator:smoke -- https://apex-lifespan.vercel.app --expect-auth-unavailable`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
   In progress 2026-06-11: added a protected read-only operator audit trail panel and bounded `getOperatorAuditTrailSnapshot` helper. The panel is visible only to active operators with `audit:read`, displays recent action/actor/target/timestamp/note previews without dumping audit JSON summaries, and anonymous operator smoke now fails if `Audit trail` leaks into closed public states. `npm run operator:readiness` now verifies the audit trail as a ready local artifact; current local report has 12 ready checks and the same 5 external/manual QA blockers. Validation passed: audit-trail/readiness/operator-source/smoke tests, full `npm run test`, `npm run operator:readiness`, `npm run launch:readiness`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
   In progress 2026-06-11: added render-level `/operator` role-gate tests for auth-unavailable, anonymous, auditor, reviewer, and admin states, and tightened the page so active auditors do not see the candidate review queue shell. Tests now prove auditors get only the audit trail, reviewers get the review queue plus audit trail without browser write buttons while controls are locked, and admins get promotion readiness without promotion controls while controls are locked. Current operator readiness remains blocked on the same 5 external/manual QA gates. Validation passed: operator page render tests, targeted operator boundary tests, full `npm run test`, `npm run operator:readiness`, `npm run launch:readiness`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
   In progress 2026-06-11: `npm run operator:readiness` now accepts reviewed value-free Vercel dashboard evidence for database-backed auth when `APEX_VERCEL_DATABASE_CONFIGURED_AT` and `APEX_VERCEL_OPERATOR_AUTH_CONFIGURED_AT` are both recorded, without copying `DATABASE_URL`, Auth.js, or GitHub OAuth secret values into the local shell. The current local report still has 12 ready checks and 5 blockers; a temporary-env probe with those two evidence keys proved the auth blocker drops to ready while active-operator, non-production write QA, manual flow QA, and browser-write-control approval remain blocked. Validation passed: operator readiness/env/launch tests, current and temporary-env `npm run operator:readiness`, `npm run launch:readiness`, targeted operator/public-boundary tests, full `npm run test`, `npm run dev:stop`, `npm run typecheck`, `npm run build`, `npm run lint`, and `git diff --check`.
   In progress 2026-06-11: added `worksheet.copySafeCommands` to `npm run operator:readiness` so the blocked local report emits exact safe commands for refreshing operator readiness, checking anonymous `/operator` boundaries, dry-running an operator bootstrap plan, and refreshing launch readiness. The packet intentionally contains no `--apply` command; the emitted bootstrap command was run and returned `applied=false`, `dryRun=true`, and the live public operator smoke stayed closed with `--expect-auth-unavailable`. Validation passed: operator readiness tests, `npm run operator:readiness`, emitted dry-run bootstrap command, `npm run operator:smoke -- https://apex-lifespan.vercel.app --expect-auth-unavailable`, `npm run launch:readiness`, targeted operator/public-boundary tests, full `npm run test`, `npm run dev:stop`, `npm run typecheck`, `npm run build`, and `npm run lint`.
   In progress 2026-06-11: added focused read-only operator readiness packets via `npm run operator:readiness -- --check <check-id>` and surfaced the placeholder command in the operator worksheet. The focused packet prints one operator check with status, evidence keys, available ids, and next action; missing ids and missing `--check` values fail closed. Current local readiness still has 12 ready checks and the same 5 external/manual QA blockers, with no new browser write exposure. Validation passed: operator readiness tests, focused `operator-auth-config` and `operator-write-gate` packets, expected fail-closed missing-check and bad-argument cases, aggregate operator readiness, launch readiness summary, targeted launch/public-boundary tests, `npm run dev:stop`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
   In progress 2026-06-11: added compact read-only operator readiness summaries via `npm run operator:readiness -- --summary` and surfaced the command in the operator worksheet. The summary reports counts, ready local artifacts, ready evidence, blockers, warnings, and next action without dumping the full check list or command packet. Current local summary still has 12 ready checks, 5 external/manual QA blockers, and 0 warnings; `--summary` cannot be combined with `--check`, and unknown args fail closed. Validation passed: operator readiness tests, aggregate/focused/summary CLI checks, expected fail-closed summary/check and unknown-argument cases, launch summary, targeted launch/public-boundary tests, full tests, `npm run dev:stop`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
   Remaining blocker 2026-06-11: completing this step requires configured database-backed GitHub OAuth or reviewed Vercel dashboard evidence, at least one seeded active operator account, `APEX_OPERATOR_WRITES_ENABLED=true` in a non-production environment, and manual operator-flow QA before browser write controls are exposed.

7. [ ] Implement human-reviewed curation promotion.
   Done when: accepted candidates can become public evidence packets only after claim link, structured extraction, citation traceability, and human review are complete.
   Validate with: candidate-to-public promotion tests, source-packet tests, and a dry-run promotion of `PMID 42141930` before any public promotion.
   In progress 2026-06-11: added `docs/codex/plans/2026-06-11-curation-promotion.md`, read-only promotion assessment, and `npm run promotion:dry-run -- --pmid <id>`. Unit tests prove promotion is blocked until accepted candidates are human-reviewed, claim-linked, structurally extracted, citation-traceable, and `publicSourcePacketReady=true`. Dry run for `PMID 42141930` passed as a blocked assessment: accepted/human-reviewed candidate found, but claim link, structured extraction, and public packet readiness are still missing. Validation passed: promotion tests, source-packet tests, public-boundary tests, `npm run typecheck`, `npm run build`, `npm run lint`, and `npm audit`.
   In progress 2026-06-11: added `docs/codex/plans/2026-06-11-promotion-readiness-panel.md`, a bounded promotion-readiness snapshot helper, and a protected `/operator` read-only panel for admin+ operators. The panel lists accepted candidates, readiness counts, next actions, and blockers without adding promote, accept/reject, claim-link, extraction, or other browser write controls. Dry run for `PMID 42141930` still passed as blocked for missing claim link, structured extraction, and `publicSourcePacketReady=true`. Validation passed: promotion readiness tests, operator auth-control tests, auth-route/public-boundary/operator-session tests, broader operator/public-boundary bundle, `npm run promotion:dry-run -- --pmid 42141930`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
   In progress 2026-06-11: added `docs/codex/curation-promotion-checklist.md` and linked it from `docs/codex/source-candidate-workflow.md` so human reviewers have an explicit pre-promotion checklist for candidate state, curation draft, claim link, structured extraction, citation traceability, dry-run output, and evidence to record. Dry run for `PMID 42141930` still passed as a blocked assessment with the same three blockers: claim link missing, structured extraction missing, and `publicSourcePacketReady=false`. Validation passed: promotion/source-packet/public-boundary tests, `npm run promotion:dry-run -- --pmid 42141930`, and `git diff --check`.
   In progress 2026-06-11: added a read-only promotion dry-run worksheet to the local assessment output so accepted `PMID 42141930` now reports the exact human next actions for claim linking, structured extraction, and rerunning the dry run before any explicit promotion review. The assessment still blocks promotion while `publicSourcePacketReady=false`; no public route, browser write control, or auto-promotion path was added. Validation passed: promotion assessment tests, source-packet/public-boundary tests, `npm run promotion:dry-run -- --pmid 42141930`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
   In progress 2026-06-11: added an audited admin-only public evidence promotion wrapper plus a gated browser promotion form. The wrapper reuses the dry-run assessment, refuses unready candidates, marks only the target claim as `Human reviewed`, records `lastReviewedAt`, and appends an operator audit event; the browser form stays hidden unless the accepted candidate row is ready and the existing operator browser-control QA gate is approved. Current dry run for `PMID 42141930` remains blocked by missing claim link, structured extraction, and `publicSourcePacketReady=false`, so no promotion is available in the current local state. Validation passed: full `npm run test` (43 files, 415 tests), targeted operator/promotion and public-boundary tests, `npm run promotion:dry-run -- --pmid 42141930`, `npm run operator:readiness`, `npm run launch:readiness`, `npm run lint`, `npm audit`, `npm run dev:stop`, `npm run typecheck`, `npm run build`, and `git diff --check`.
   In progress 2026-06-11: added `worksheet.readOnlyCommands` to `npm run promotion:dry-run` output so accepted `PMID 42141930` now emits shell-safe inspection commands for review packet, reference matches, siblings, curation status, curation draft, and rerunning the promotion dry run. The real dry run remains blocked by missing claim link, structured extraction, and `publicSourcePacketReady=false`; the emitted curation-status command was executed successfully and made no writes. Validation passed: full `npm run test`, promotion/action tests, `npm run promotion:dry-run -- --pmid 42141930`, the emitted curation-status command, `npm run launch:readiness`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
   In progress 2026-06-11: added `worksheet.requiredEvidence` to `npm run promotion:dry-run` so the dry-run now shows the human evidence gates directly: candidate accepted, candidate human-reviewed, accepted reference traceable, claim link, structured extraction, and public source packet readiness. The current dry run for `PMID 42141930` reports the first 3 gates ready and the claim-link, structured-extraction, and public-packet gates blocked; no source-candidate writes, public routes, or auto-promotion paths changed. Validation passed: promotion tests, source-candidate/public-boundary tests, `npm run promotion:dry-run -- --pmid 42141930`, `npm run launch:readiness`, full `npm run test`, `npm run dev:stop`, `npm run typecheck`, `npm run lint`, `npm audit`, `npm run build`, and `git diff --check`.
   In progress 2026-06-11: added read-only `npm run promotion:readiness` for an accepted-candidate promotion snapshot without requiring an authenticated operator page. The report is human-owned/read-only, groups blocked and ready promotion rows, emits only copy-safe inspection commands, and supports bounded `--limit`. Current local output shows 1 blocked accepted candidate (`PMID 42141930`) with the same three blockers: claim link, structured extraction, and `publicSourcePacketReady=true`; no source-candidate writes, public route, or auto-promotion path changed. Validation passed: promotion report tests, promotion/action/public-boundary tests, `npm run promotion:readiness`, `npm run promotion:readiness -- --limit 1`, expected fail-closed `npm run promotion:readiness -- --limit 0`, `npm run promotion:dry-run -- --pmid 42141930`, `npm run launch:readiness -- --summary`, `npm run dev:stop`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
   Remaining blocker 2026-06-11: using the promotion write/control still requires human-owned claim link and structured extraction for `PMID 42141930`, configured operator auth, non-production manual QA, and browser-control approval; public routes remain read-only and no auto-promotion path exists.

8. [ ] Add scheduled source ingestion.
   Done when: PubMed and ClinicalTrials.gov ingestion jobs run on a schedule with rate limits, retries, dedupe, failure reporting, dry-run support, and no automatic public promotion.
   Validate with: scheduler dry run, integration tests with mocked upstreams, and source policy/rate-limit review.
   In progress 2026-06-11: added `docs/codex/scheduled-ingestion-design.md`, a read-only scheduler dry-run planner, and `npm run ingest:scheduled-dry-run`. Dry run reports queued/running/failed state, planned batch size, and `noAutoPromotion=true` without queueing or running jobs. Local dry run passed with no queued jobs. Validation passed: scheduler dry-run tests, public-boundary tests, `npm run typecheck`, `npm run build`, `npm run lint`, and `npm audit`.
   In progress 2026-06-11: added `docs/codex/plans/2026-06-11-scheduled-ingestion-policy.md` and a structured policy review in `npm run ingest:scheduled-dry-run`. The dry run now reports PubMed `retmax` cap 20, ClinicalTrials.gov `pageSize` cap 20, scheduler default/max batch 1/5, `automaticRetries=false`, `noAutoPromotion=true`, and missing NCBI metadata by variable name only. Local dry run passed with no queued/running/recent failed jobs and `hostedCronReady=false` because `NCBI_EMAIL` is missing. Validation passed: scheduler/public-boundary tests, `npm run ingest:scheduled-dry-run`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
   In progress 2026-06-11: added `docs/codex/plans/2026-06-11-scheduled-ingestion-runner.md` and guarded local `npm run ingest:scheduled-run`. The command plans first, requires `--apply`, `APEX_SCHEDULED_INGESTION_WRITES_ENABLED=true`, `NCBI_TOOL`, `NCBI_EMAIL`, and no running ingestion job before executing a bounded batch through the existing source-candidate runner with PubMed/ClinicalTrials.gov caps of 20. Tests use a mocked runner to prove dry-run default, write-gate blocking, metadata blocking, running-job blocking, capped execution, `automaticRetries=false`, and `noAutoPromotion=true`. Local `npm run ingest:scheduled-run` correctly failed closed with exit code 1 because `APEX_SCHEDULED_INGESTION_WRITES_ENABLED` is not enabled. Validation passed: scheduler/public-boundary tests, `npm run ingest:scheduled-dry-run`, fail-closed `npm run ingest:scheduled-run`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
   In progress 2026-06-11: added `docs/codex/plans/2026-06-11-scheduled-ingestion-hosted-readiness.md` and expanded the dry-run policy report with a hosted-cron readiness contract. The report now keeps `hostedCronReady=false` until managed database mode, managed `DATABASE_URL`, scheduled write gate, ingestion alerts, explicit cron approval, and NCBI metadata are configured; it reports missing evidence by variable/setting name only and does not add a hosted write endpoint. Current local dry run remains blocked as expected with no queued jobs and missing hosted-cron evidence for `DATABASE_URL`, `APEX_DATA_SOURCE=database`, `APEX_SCHEDULED_INGESTION_WRITES_ENABLED=true`, `APEX_INGESTION_ALERTS_CONFIGURED=true`, `APEX_SCHEDULED_INGESTION_CRON_APPROVED=true`, and `NCBI_EMAIL`. Validation passed: scheduler/public-boundary tests, `npm run ingest:scheduled-dry-run`, fail-closed `npm run ingest:scheduled-run`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
   In progress 2026-06-11: added `docs/codex/plans/2026-06-11-scheduled-ingestion-failure-review.md` and a read-only failed-job retry review inside `npm run ingest:scheduled-dry-run`. The review classifies recent failed jobs into sanitized retry categories without printing raw failed-job errors, keeps `automaticRetries=false`, and keeps `retryAutomationReady=false` until failure classes and a retry policy are explicitly reviewed. Current local dry run reports `failedJobsReviewed=0`. Validation passed: scheduler/public-boundary tests, `npm run ingest:scheduled-dry-run`, fail-closed `npm run ingest:scheduled-run`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
   In progress 2026-06-11: added a read-only scheduled ingestion worksheet to `npm run ingest:scheduled-dry-run`. The worksheet groups ready local controls, queued work, hosted-cron blockers, retry-policy blockers, warnings, and the next operator-owned action while keeping `humanOwned=true`, `automaticRetries=false`, and `noAutoPromotion=true`. Current local dry run reports 0 queued jobs, 0 failed jobs, ready dry-run/source-cap/no-auto-promotion/manual-retry controls, and blockers for `NCBI_EMAIL`, hosted cron evidence, and retry policy approval; `npm run ingest:scheduled-run` still fails closed because `APEX_SCHEDULED_INGESTION_WRITES_ENABLED=true` is not set. Validation passed: scheduler/launch-readiness tests, `npm run ingest:scheduled-dry-run`, fail-closed `npm run ingest:scheduled-run`, and `npm run launch:readiness`.
   In progress 2026-06-11: added a bounded read-only dedupe review to `npm run ingest:scheduled-dry-run`. The report now summarizes duplicate PMID/NCT source identities, decision counts, mixed-decision state, and a read-only inspection command without merging, accepting, rejecting, retrying, or promoting candidates. Current local dry run reports 1 duplicate PubMed identity group for `PMID 42141930` with 1 pending and 1 accepted candidate, and the generated duplicate-inspection command passed. Validation passed: scheduler/launch-readiness tests, `npm run ingest:scheduled-dry-run`, `npm run ingest:sources -- --candidates --candidate-duplicates --candidate-source pubmed --candidate-external-id 42141930`, and `npm run launch:readiness`.
   In progress 2026-06-11: added `docs/codex/scheduled-ingestion-retry-policy.md` and `APEX_INGESTION_RETRY_POLICY_APPROVED_AT` as the explicit retry-policy approval evidence gate. The dry run now reports `failureReview.retryPolicy`, keeps `automaticRetries=false`, and marks retry readiness true only when policy approval is recorded and no recent failed jobs await review. Current local dry run remains blocked on missing `NCBI_EMAIL`, hosted cron evidence, and `APEX_INGESTION_RETRY_POLICY_APPROVED_AT`; `npm run ingest:scheduled-run` still fails closed with no writes because the scheduled write gate is disabled. Validation passed: scheduler/launch/env tests, `npm run ingest:scheduled-dry-run`, fail-closed `npm run ingest:scheduled-run`, and `npm run launch:readiness`.
   In progress 2026-06-11: added `worksheet.copySafeCommands` to `npm run ingest:scheduled-dry-run` so the scheduler handoff emits exact read-only/dry-run commands for scheduler refresh, source-candidate DB status, queued/failed job review, duplicate identity review, pending candidate overview, and aggregate launch readiness. The packet intentionally contains no `--apply` command. Current local dry run reports 0 queued jobs, 0 failed jobs, 1 duplicate PubMed identity group for `PMID 42141930`, missing `NCBI_EMAIL`, hosted-cron evidence, and retry-policy approval; the guarded `npm run ingest:scheduled-run` still fails closed with no writes. Validation passed: scheduled-ingestion tests, dry run, all emitted read-only source-candidate commands, fail-closed guarded run, `npm run launch:readiness`, launch/public-boundary tests, full `npm run test`, `npm run dev:stop`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
   In progress 2026-06-11: added an opt-in hosted-run readiness gate to the scheduled ingestion batch runner for a future cron route without exposing a hosted HTTP write endpoint. Local CLI behavior is unchanged; hosted-run mode now refuses to process queued jobs unless hosted cron evidence and retry-policy readiness are present, while preserving bounded source caps, no automatic retries, and no public evidence promotion. Current local dry run still reports 0 queued jobs, 0 failed jobs, 1 duplicate PubMed identity group for `PMID 42141930`, and the same missing hosted/retry evidence; the guarded local runner still fails closed with writes disabled. Validation passed: scheduled-ingestion/public-boundary/launch tests, `npm run ingest:scheduled-dry-run`, fail-closed `npm run ingest:scheduled-run`, `npm run launch:readiness`, full `npm run test`, `npm run dev:stop`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
   In progress 2026-06-11: surfaced the hosted-run gate in `npm run ingest:scheduled-dry-run` as a read-only `hostedRunGate` report and worksheet item, and made aggregate launch readiness require that gate before scheduled ingestion can be launch-ready. The report states that local CLI behavior is unchanged, no hosted write route is exposed, hosted runs require explicit apply plus `requireHostedRunReadiness`, and future hosted execution must have hosted-cron plus retry-policy readiness before processing queued jobs. Validation passed: scheduled-ingestion/launch-readiness/public-boundary tests, `npm run ingest:scheduled-dry-run`, fail-closed `npm run ingest:scheduled-run`, `npm run launch:readiness`, full `npm run test`, `npm run dev:stop`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
   In progress 2026-06-11: added compact read-only scheduled-ingestion summaries via `npm run ingest:scheduled-dry-run -- --summary` and surfaced the command in the scheduler worksheet. The summary reports queue/failure/dedupe counts, hosted-cron readiness, hosted-run gate readiness, retry readiness, blockers, warnings, and next action without dumping full queue details. Current local summary still has 0 queued jobs, 0 failed jobs, 1 duplicate PubMed identity group for `PMID 42141930`, missing `NCBI_EMAIL`, missing hosted-cron evidence, and missing retry-policy approval; `--summary` cannot be combined with `--apply`, and bad scheduler args fail closed. Validation passed: scheduled-ingestion tests, compact summary, aggregate dry run, `--max-jobs 2 --summary`, expected fail-closed `--apply --summary`, expected fail-closed bad `--max-jobs`, expected fail-closed unknown arg, guarded run, launch summary, launch/public-boundary tests, full tests, `npm run dev:stop`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
   Remaining blocker 2026-06-11: hosted cron execution, retry-policy approval, alerting, unattended PubMed metadata, and production database/secrets require external setup and operator approval before enabling scheduled writes.

9. [ ] Expand evidence and intervention coverage.
   Done when: priority interventions and claims have reviewed evidence packets, clear population/dose/form boundaries, and visible uncertainty labels.
   Validate with: seed/database integrity checks, source-packet tests, review sampling, and dashboard QA.
   In progress 2026-06-11: added read-only evidence coverage summary and `npm run coverage:review`. Current local report shows 7/7 structurally complete source packets, 0 human-reviewed claims, 7 unreviewed draft claims, and 1 intervention without claim coverage (`psyllium`). Validation passed: coverage test, source-packet tests, seed-integrity tests, `npm run typecheck`, `npm run build`, `npm run lint`, and `npm audit`.
   In progress 2026-06-11: added `docs/codex/plans/2026-06-11-evidence-coverage-review-backlog.md` and a ranked read-only `claimReviewBacklog` plus `interventionGaps` in `npm run coverage:review`. Current local report ranks 7 unreviewed complete source packets for human review, with top priority ties for `bpc-157-injury-healing` (regulatory concern label) and `vitamin-d-deficiency` (safety outcome/high-confidence draft), and keeps `psyllium` as the only intervention gap. No claim review statuses or public evidence records were changed. Validation passed: coverage/source-packet/seed-integrity tests, dashboard/public-boundary tests, `npm run coverage:review`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
   In progress 2026-06-11: added a read-only `reviewSamplingPlan` to `npm run coverage:review` so the next human review batch is explicit without changing claim status or public evidence. Current local sampling plan selects 3 of 7 complete unreviewed packets in priority order: `bpc-157-injury-healing`, `vitamin-d-deficiency`, and `creatine-strength`, with linked reference IDs and structured study IDs included for each. Validation passed: coverage/source-packet/seed-integrity tests, dashboard/public-boundary tests, `npm run coverage:review`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
   In progress 2026-06-11: added a read-only human coverage worksheet to `npm run coverage:review`. The worksheet groups the first 3-claim review batch, all 7 ready source packets, the remaining backlog, and the `psyllium` intervention gap while keeping coverage work human-owned and leaving claim review statuses/public evidence unchanged. Current local report still shows 0/7 human-reviewed claims, 7 ready source packets, and 1 intervention gap. Validation passed: coverage/launch-readiness tests, `npm run coverage:review`, source-packet/seed-integrity/dashboard tests, public-boundary tests, and `npm run launch:readiness`.
   In progress 2026-06-11: added bounded review context to each sampled `coverage:review` claim: source-packet status, population/dose-form/duration boundary fields, visible confidence/final-label/review-status fields, and a short human checklist with safety, AU/TGA, peptide, and traceability reminders where relevant. No claim review statuses, source packets, public evidence, or write paths changed. Validation passed: coverage/source-packet/seed-integrity/dashboard/public-boundary/launch-readiness tests, `npm run coverage:review`, `npm run launch:readiness`, full `npm run test`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
   In progress 2026-06-11: added `worksheet.copySafeCommands` to `npm run coverage:review` so the human evidence workflow emits exact read-only commands for refreshing coverage, AU/TGA review, pending source-candidate overview, flagged source-candidate review, accepted-candidate curation handoff, and aggregate launch readiness. The packet intentionally excludes accept/reject, claim-link, extraction, queue/run, and apply commands. Current local report still has 7 complete unreviewed source packets, 0 human-reviewed claims, 1 `psyllium` intervention gap, 3 flagged candidate-review groups, and accepted `PMID 42141930` blocked on claim link/structured extraction before public packet use. Validation passed: coverage and launch-readiness tests, `npm run coverage:review`, all emitted read-only commands, source-packet/seed-integrity/dashboard/public-boundary tests, full `npm run test`, `npm run dev:stop`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
   In progress 2026-06-11: added claim-scoped read-only coverage review packets via `npm run coverage:review -- --claim <claim-id>` and surfaced the placeholder command in the coverage worksheet. The focused packet prints one claim's backlog row, population/dose/form/duration boundary, review checklist, linked reference IDs, and structured study IDs without changing claim review status, source packets, or public evidence. Validation passed: coverage/source-packet/seed-integrity/dashboard/public-boundary/launch-readiness tests, `npm run coverage:review`, `npm run coverage:review -- --claim creatine-strength`, expected fail-closed `npm run coverage:review -- --claim missing-claim`, `npm run launch:readiness`, full `npm run test`, `npm run dev:stop`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
   Remaining blocker 2026-06-11: actual coverage expansion requires human evidence review/curation before claims can become human-reviewed or new public evidence is added.

10. [x] Add product-level AU/TGA verification workflow.
    Done when: product-level ARTG/AUST evidence can be tracked separately from ingredient-level evidence, with confidence labels and stale/unknown states.
    Validate with: regulatory data tests, UI review of unknown/stale states, and no inference from generic intervention evidence.
    Completed 2026-06-11: added `docs/codex/plans/2026-06-11-product-level-au-tga-verification.md`, read-only product-level verification helpers, `npm run regulatory:review`, and dashboard product chips that show product-level AU/TGA state plus confidence without adding write paths. Current local report shows 5 intervention-level statuses, 2 product-level statuses, 2 very-low-confidence unknown product statuses, no missing product statuses, and no stale statuses. Tests prove product-level missing/stale states are not satisfied by intervention-level AU/TGA records. Validation passed: `npm run test -- src/lib/australia-regulatory-verification.test.ts src/components/evidence-dashboard.test.tsx src/lib/regulatory.test.ts`, `npm run regulatory:review`, public read-only boundary tests, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.

11. [ ] Add observability, backups, privacy, and operations.
    Done when: uptime/error monitoring, deployment alerts, database backups/restore rehearsal, privacy/terms copy, and operator runbooks are in place.
    Validate with: alert test, backup restore rehearsal, docs review, and deployment rollback drill.
    In progress 2026-06-11: added `docs/codex/plans/2026-06-11-operations-privacy-readiness.md`, public `/privacy` and `/terms` pages, dashboard header links to those pages, and `docs/codex/operations-runbook.md` covering monitoring, deployment alerts, database backup/restore rehearsal, rollback, privacy/terms alignment, and launch evidence. Validation passed: legal page/dashboard render tests, public read-only boundary tests, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
    In progress 2026-06-11: added `docs/codex/plans/2026-06-11-operations-readiness-report.md`, sanitized read-only `npm run operations:readiness`, and unit coverage for ready, blocked, and missing-local-artifact states. Current local report is blocked as expected with 3 local artifacts ready and 8 external operations evidence items missing: uptime monitoring, error monitoring, deployment alerts, scheduled-ingestion alerts, database backups, backup restore rehearsal, rollback drill, and alert test. Validation passed: `npm run test -- src/lib/operations-readiness.test.ts`, `npm run operations:readiness`, targeted operations/legal/public-boundary tests, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
    In progress 2026-06-11: added a read-only operations evidence worksheet to `npm run operations:readiness`. The report now groups ready local artifacts, missing external evidence, ready external evidence, and the next operator-owned evidence action without printing secret values. Current local report remains blocked with 3 ready local artifacts and 8 missing external operations evidence items; the first next action is uptime monitoring configuration. Validation passed: operations/launch-readiness tests, `npm run operations:readiness`, `npm run launch:readiness`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
    In progress 2026-06-11: added `docs/codex/operations-drill-checklist.md`, a human-owned drill order for uptime/error monitoring, Vercel deployment alerts, ingestion alerts, managed backups, non-production restore rehearsal, rollback rehearsal, and alert testing. `npm run operations:readiness` now verifies the checklist as a local artifact. Current local report has 4 ready local artifacts and remains blocked on the same 8 external evidence items, with uptime monitoring still first. Validation passed: operations/launch-readiness tests, `npm run operations:readiness`, and `npm run launch:readiness`.
    In progress 2026-06-11: added public read-only `/api/health` for lightweight uptime monitoring. The endpoint returns a no-store/noindex static health payload without checking the database or reading secrets; public smoke now validates it, and `npm run operations:readiness` counts it as a ready local artifact while still blocking on external uptime/error/deployment/backup/rollback evidence. Current local report has 5 ready local artifacts and 8 missing external operations evidence items, with `/api/health` uptime monitoring first. Validation passed: health route, public smoke, operations-readiness, and public read-only boundary tests; `npm run operations:readiness`; and `npm run launch:readiness`.
    In progress 2026-06-11: tightened uptime evidence hygiene without adding new evidence keys. `npm run operations:readiness` now rejects `APEX_UPTIME_MONITORING_URL` values that are unparsable, non-http(s), local-only, or include credentials, query strings, or fragments, and it does not echo token-bearing URLs in reports. Clean sanitized monitor URLs still satisfy the uptime evidence check. Validation passed: operations-readiness tests, `npm run operations:readiness`, and `npm run launch:readiness`.
    In progress 2026-06-11: deployed the read-only operations hardening to the public seed-mode demo by fast-forwarding `main` to `70a6da5` and pushing to GitHub. Vercel caught up on the third smoke attempt, then a clean `npm run smoke:public-mvp -- https://apex-lifespan.vercel.app` passed homepage, `/privacy`, `/terms`, anonymous `/operator`, `/api/health`, PubMed and ClinicalTrials.gov previews, and invalid-term guards.
    In progress 2026-06-11: added `worksheet.copySafeCommands` to `npm run operations:readiness` so the operations handoff emits exact read-only commands for refreshing operations readiness, smoking public routes and `/api/health`, refreshing production readiness, running the scheduled-ingestion dry run, and refreshing aggregate launch readiness. The packet intentionally excludes apply, queue/run, candidate decision, claim-link, and extraction commands. Current local report still has 5 ready local artifacts and 8 missing external operations evidence items, with uptime monitoring first; live public smoke passed against `https://apex-lifespan.vercel.app`. Validation passed: operations/launch/health/smoke tests, `npm run operations:readiness`, emitted read-only commands, `npm run dev:stop`, full `npm run test`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
    In progress 2026-06-11: added focused read-only operations evidence packets via `npm run operations:readiness -- --evidence <evidence-id>` and surfaced the placeholder command in the operations worksheet. The focused packet prints one operations check with its evidence keys, status, available ids, and next action; missing ids exit 1 with a not-found packet. Current local report still has 5 ready local artifacts and 8 missing external operations evidence items, with uptime monitoring first. Validation passed: operations/launch-readiness tests, `npm run operations:readiness`, `npm run operations:readiness -- --evidence uptime-monitoring`, expected fail-closed `npm run operations:readiness -- --evidence missing-evidence`, `npm run launch:readiness`, `npm run dev:stop`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
    Remaining blocker 2026-06-11: completing this step requires operator setup and proof outside the local checkout: uptime/error monitoring, Vercel deployment alerts, Neon backup/restore rehearsal, alert test, and rollback drill evidence.

12. [ ] Run full product QA and hardening.
    Done when: accessibility, mobile/desktop layout, performance, security headers, dependency audit, and regression suites pass for production data mode.
    Validate with: `npm run test`, `npm run lint`, `npm run typecheck`, `npm run build`, `npm audit`, public smoke, browser QA, and accessibility/performance checks.
    In progress 2026-06-11: added `docs/codex/plans/2026-06-11-public-security-headers.md`, conservative global security headers in `next.config.mjs`, and `src/app/security-headers-config.test.ts`. Headers cover limited CSP directives, referrer policy, HSTS, MIME sniffing, DNS prefetch, frame blocking, and unused device/browser APIs while avoiding strict script/style CSP and cross-origin isolation until browser QA. Validation passed: header config/public-boundary tests, full `npm run test` (31 files, 361 tests), `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
    In progress 2026-06-11: expanded `npm run smoke:public-mvp -- <url>` to validate homepage, `/privacy`, `/terms`, global security headers, live-source previews, and invalid-term guards. Local production seed-mode smoke passed against `http://127.0.0.1:3000`, and Playwright desktop/mobile screenshots were reviewed from `output/playwright/apex-home-desktop.png` and `output/playwright/apex-home-mobile.png` with no new header/legal-link overlap observed. Validation passed: targeted render/header tests, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
    In progress 2026-06-11: fixed local axe accessibility findings by darkening the amber status token, moving dashboard micro-labels to higher-contrast slate text, replacing an internal `aside` landmark with a section, and removing `MiniStat` cards from definition-list markup. Local production seed-mode axe checks passed with 0 violations for `/`, `/privacy`, and `/terms`; smoke and Playwright desktop/mobile screenshot review passed afterward. Validation passed: `npm run test`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
    In progress 2026-06-11: local production seed-mode Lighthouse pass completed for `/` after pointing Windows temp paths at `output/lighthouse-temp`; scores were Performance 96 and Accessibility 100. Report artifact: `output/playwright/lighthouse-home.json` (local ignored output).
    In progress 2026-06-11: expanded `npm run smoke:public-mvp -- <url>` to accept either explicit `Seed fallback` or `Database-backed` data-source badges and to assert `/operator` shows only a closed anonymous state, not review queue, promotion, or sign-out content. Local production database-backed smoke passed against `http://127.0.0.1:3000`. Live public smoke against `https://apex-lifespan.vercel.app` currently fails before route checks because the deployed homepage is missing the expected `Content-Security-Policy: frame-ancestors 'none'` header, so public deploy/header verification remains open. Validation passed: local smoke, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
    In progress 2026-06-11: refactored `scripts/smoke-public-mvp.ts` so the smoke runner can be imported by tests while preserving the CLI entrypoint, then added mock-server coverage for database-backed public smoke and for failing when anonymous users can see operator review content. Validation passed: `npm run test -- scripts/smoke-public-mvp.test.ts`, targeted operator/public-boundary tests, local CLI smoke against `http://127.0.0.1:3000`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
    In progress 2026-06-11: fast-forwarded production `main` to the validated branch tip and pushed it to trigger Vercel. `npm run smoke:public-mvp -- https://apex-lifespan.vercel.app` passed after the redeploy caught up, covering homepage security headers, `/privacy`, `/terms`, anonymous `/operator` closure, PubMed and ClinicalTrials.gov live previews, and invalid-term guards.
    In progress 2026-06-11: captured and visually reviewed live production seed-mode screenshots at `output/playwright/apex-public-desktop-2026-06-11.png` and `output/playwright/apex-public-mobile-2026-06-11.png`. Desktop and mobile renders showed the public dashboard, legal links, anonymous operator boundary, evidence map, claim table, safety cards, product panel, and source queue without obvious text collision or header/control overlap.
    In progress 2026-06-11: live production seed-mode accessibility and performance checks passed. `npx --yes @axe-core/cli https://apex-lifespan.vercel.app https://apex-lifespan.vercel.app/privacy https://apex-lifespan.vercel.app/terms --save axe-public-2026-06-11.json --dir output/playwright --exit --load-delay 1000 --timeout 120 --chrome-options="--headless=new --no-sandbox"` found 0 violations; Lighthouse homepage report `output/playwright/lighthouse-public-home-2026-06-11.json` scored Performance 98, Accessibility 100, Best Practices 100, and SEO 100.
    In progress 2026-06-11: refreshed live public seed-mode smoke after deployment troubleshooting. `npm run smoke:public-mvp -- https://apex-lifespan.vercel.app` passed homepage security/text checks, `/privacy`, `/terms`, anonymous `/operator` closure, PubMed and ClinicalTrials.gov live previews, and both invalid-term guards. This confirms the current public demo works, but it does not satisfy the production database-mode QA blocker.
    Remaining blocker 2026-06-11: full step completion still requires production database mode, production database-mode public smoke, browser QA, and accessibility/performance confirmation, plus any external security-header scanner or monitoring evidence chosen for launch.

13. [ ] Launch the fully live product and monitor post-launch.
    Done when: production database mode is live, authenticated workflows are operational, scheduled ingestion is monitored, public smoke passes, rollback is rehearsed, and post-launch issues are tracked.
    Validate with: launch checklist, public URL smoke, admin-flow smoke, monitoring confirmation, and a 24-48 hour post-launch review.
    In progress 2026-06-11: added `docs/codex/plans/2026-06-11-launch-readiness-report.md` and read-only `npm run launch:readiness`, which aggregates production, operator, operations, scheduled ingestion, promotion, coverage, public smoke, admin smoke, launch approval, and post-launch review evidence into one launch gate report. Current local report is blocked as expected with 10 blocked launch checks, including production database/secrets, operator QA, operations evidence, hosted cron/retry policy, accepted-candidate promotion blockers, human-reviewed coverage, fully-live public/admin smokes, launch approval, and post-launch review scheduling. Validation passed: launch-readiness tests, targeted readiness/scheduler/promotion/coverage tests, `npm run launch:readiness`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
    In progress 2026-06-11: documented fully-live evidence variables in `.env.example` for operator QA, migration rehearsal, operations evidence, and launch/post-launch checks, with defaults commented or false so public deployments do not accidentally claim readiness. Added test coverage to keep the operator template in sync with readiness evidence keys. Validation passed: `.env.example` evidence-variable test, `npm run typecheck`, `npm run lint`, and `git diff --check`.
    In progress 2026-06-11: added `docs/codex/fully-live-launch-checklist.md` and made `npm run launch:readiness` verify the checklist artifact exists before launch approval. The checklist covers preflight, production data, operator workflow, human evidence gates, scheduled ingestion, operations evidence, launch QA, approval, and post-launch follow-up without storing secrets or enabling writes. Current local launch readiness now reports the checklist as ready and remains blocked on the same 10 real launch gates. Validation passed: launch-readiness tests, `npm run launch:readiness`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
    In progress 2026-06-11: added a read-only launch readiness worksheet to `npm run launch:readiness`. The worksheet groups blocked gates, ready gates, warning gates, and the next launch action without printing evidence values. Current local report has 1 ready gate (`fully-live-launch-checklist`), 10 blocked gates, and the next launch action remains production data/secrets. Validation passed: launch-readiness tests and `npm run launch:readiness`.
    In progress 2026-06-11: added `docs/codex/post-launch-review-template.md` for the 24-48 hour fully-live review and made `npm run launch:readiness` verify it as a required local artifact. Current local report has 2 ready gates (`fully-live-launch-checklist`, `post-launch-review-template`), 10 blocked launch gates, and the next launch action remains production data/secrets. Validation passed: launch-readiness tests and `npm run launch:readiness`.
    In progress 2026-06-11: `npm run launch:readiness` now bubbles up concrete nested readiness next actions instead of generic blocked-gate wording. Current local report still has 2 ready gates and 10 blocked gates, but the top-level next launch action now points directly to the production database step: use a managed non-local PostgreSQL target before production migration rehearsal. Validation passed: launch-readiness tests, `npm run launch:readiness`, full `npm run test`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
    In progress 2026-06-11: added `worksheet.copySafeCommands` to `npm run launch:readiness` so the aggregate launch handoff emits exact read-only commands for launch, production, operator, operations, scheduled-ingestion dry run, coverage review, promotion dry run, public smoke, and anonymous operator smoke checks. The packet intentionally excludes apply commands, scheduled-run writes, operator bootstrap, candidate decision, claim-link, and extraction commands. Current local report still has 2 ready gates and 10 blocked gates; emitted commands passed with the live seed-mode URL and accepted `PMID 42141930` where placeholders were needed. Validation passed: launch-readiness tests, `npm run launch:readiness`, emitted read-only commands, `npm run dev:stop`, full `npm run test`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.
    In progress 2026-06-11: added compact read-only launch summaries via `npm run launch:readiness -- --summary` and surfaced the command in the launch worksheet. The summary reports counts, ready gates, blocked gates, warning gates, and the next launch action without dumping full nested readiness payloads. Current local summary still has 2 ready gates and 10 blocked gates, with production database/Vercel evidence first. Validation passed: launch-readiness tests, neighboring readiness tests, `npm run launch:readiness -- --summary`, expected unknown-argument failure, `npm run launch:readiness`, `npm run production:readiness -- --check database-url`, `npm run dev:stop`, `npm run typecheck`, `npm run build`, `npm run lint`, `npm audit`, and `git diff --check`.

## Automatic Rollover Rule

When step 13 is complete, do this in the same local change:

1. Move this roadmap to `docs/codex/archive/roadmap/YYYY-MM-DD-fully-live-end-product.md`.
2. Create a fresh `docs/codex/roadmap.md` for the next operating phase.
3. Seed the successor roadmap with ordered steps for coverage growth, quality operations, user feedback, analytics, and reliability improvements.
4. Update `docs/codex/handoff.md` with the archive path and new roadmap target.
5. Commit and push when useful.
